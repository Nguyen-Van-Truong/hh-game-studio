{
  "name": "hh-godot-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "HH Godot Agent sidecar. Action registry plus stdio MCP and loopback session transport.",
  "engines": {
    "node": "24.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --pretty false",
    "generate": "tsc -p tsconfig.json --pretty false && node dist/generate.js"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
