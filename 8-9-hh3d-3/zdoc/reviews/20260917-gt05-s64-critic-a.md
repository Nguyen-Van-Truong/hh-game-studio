**GT-05 S64 — independent critic A**

Date: 2026-09-17. Reviewer: `/root/gt05_critic_a_s63`.

VERDICT=PASS

TICK=yes

This verdict supports coordinator acceptance of GT-05 only for the frozen,
fixed original fixture pipeline and protected immutable staged snapshots on
the demonstrated Windows host. No unresolved material finding remains within
that scope. The coordinator retains plan/tick authority.

| Binding | Exact value |
| --- | --- |
| Reviewed commit | `808d8ba94e3c39b19c8381072775590b7cafdc0c` |
| Manifest | `zdoc/reviews/20260917-gt05-s63-audit/manifest.json` |
| Manifest SHA256 | `fd0b4a984c7bee261c083c07d461baf55325e936d66df2eeab897b422ee0a02b` |
| Source closure, 143 files | `c141d54b80022ca7fefdf81f83e7e1078d3cc6aa43954831f82bb3178338a135` |
| Review closure, 13 files | `21061e5e6c8cae1ab7bcb724f2c21dace72c6d1198d0ecab70b7c32e9a4c12e1` |
| Raw closure, 1722 files | `1cd71ca1c2ae173f7bc4701e25f44ed35e0750a426bafd7afc8bc560be99feb5` |

The three closure hashes use sorted UTF-8 `path + NUL + per-file SHA256 + LF`
records. Their path domains are defined by the manifest. They are distinct
from the publication provider's JCS source-map digest and exact report-file
hashes. The folder's S63 name is retained; the selected manifest, S64 runs and
`requirements-s64.md` identify this verdict.

I did not implement or modify the candidate source, launch an engine, stage or
commit files, or read another critic's verdict. I read the scoped AGENTS and
GT-05 contract, sections 2.3–2.5, TQ00/TQ05 and TX04/07/08/09. Accepted dependency
receipts were treated as scoped reuse; their signatures were not transferred
to this pipeline. This report is my sole workspace write.

Actual independent checks:

1. Recomputed every source/review/raw file SHA256 and all three closure hashes:
   zero mismatches. Independently read all 157 Git blobs, including the manifest,
   directly from the full commit above using `git cat-file --batch`; every blob
   matched. HEAD was checked before and after and remained that commit.
2. Ran `python -B 8-9-hh3d-3/zdoc/reviews/20260917-gt05-s63-audit/verify.py --git-ref HEAD`:
   verified=true, exit 0. The CLI accepts HEAD/index, so its result is additionally
   bound by the direct exact-commit check above. Ran the sibling `test_verify.py`:
   18/18 PASS, exit 0. No report flag alone was accepted as process evidence.
3. Independently called final installed `verify_chain` using
   `gt05-publication-s64-02/invocation.json`, with current source required. It
   completed in 20.093 seconds, exit 0, and returned a proof exactly equal to
   the captured proof: 770 reads, nine evidence roles, complete=true,
   publishable=true, no stale source. These comprise 47 source reads and 723
   local artifacts, not 770 separate raw artifacts. The exact saved proof hash
   is `4239fd7586d0106b6d363e4534e77163829f0c555ca74a1f6035dfab313769e7`.
4. Audited closed input schemas, fixed artifact names, bounded GLB/accessor/PNG
   admission, pinned Khronos and engine captures, semantic rejection, source
   and payload bindings, immutable provider output, lease/source/deadline
   guards, global command routing, exact retry, protected readback and custody.
   The reducer requires ordered witnessed phases. Stop signals the provider
   outside the publisher mutex and is checked through verification and effect
   boundaries. Ambiguous effects do not become an inferred successful receipt.
5. Checked final full-suite evidence: `gt05-units-s64-02`, 233 run, 232 pass,
   zero failures/errors, one explicitly recorded symlink-creation permission
   skip. Raw test roster/marker, stderr result, host PID/exit and frozen source
   bindings are verified. I did not rerun this complete suite. I separately
   executed the eight pure nav regression methods and the two new log-admission
   methods during this review: all ten passed with process exit 0.
6. Checked `gt05-snapshot-native-s64-02` and `gt05-staged-cut-s64-02`:
   11 native cases plus actual death after witnessed STAGED. Seven matrix
   children and the STAGED child exit 86 intentionally; successful supervising
   workers exit 0. Preterminal recovery remains UNKNOWN, unwitnessed terminal
   suffix remains HELD, witnessed terminal is COMMITTED. The STAGED supplement
   rereads the complete protected prefix, has no selector, rejects selected
   readback and preserves the prior completed snapshot. These tests use
   synthetic storage payloads and do not establish asset semantics themselves.
7. Checked `gt05-publication-s64-02`: the installed provider verifies the full
   asset chain, publishes four actual payloads, reads protected bytes back,
   returns exact same-owner/cross-owner retries without repeating the provider,
   and reopens read-only. Final receipt SHA256 is
   `261d4ba19c7ba985d1201c9aea9f1668c4a5c73d801f50954d77318426e58403`.
   Baseline GLB SHA256 is
   `e85e536137208ba84adbc875b6fb647b5a4e49156344e03e2e8947a7ae91e377`;
   baseline source .blend SHA256 is
   `ad88ee8d6c15d85acc7332d6896818f82309869a57c9cf4667ea91019a245956`.

My initial blocking finding was missing actual nav-path validation: S63 had
only an enabled region and geometry counts. The final four-file nav repair and
`consumer-s64-02`, `visual-s64-01`, `reimport-s64-01` evidence close it. I read
their raw observations and host captures and recomputed the comparator. Each
records map iteration 0, then 1 without endpoint owners, then 2 with the exact
region owner. Only the imported proxy's top is admitted: four vertices, two
polygons, 24 square metres. Two actual three-point diagonal queries reach their
specified endpoints, remain on y=0, have UP normals and the correct region at
every point, and each measures approximately 5.83095169 metres. The negative
tests reject missing paths, premature map readiness, foreign owners, partial
endpoints, wrong normals, displaced points and box sides/bottom. No tolerance
was relaxed. The separate log fix now rejects `Error: message`; its two
regressions also distinguish benign counters.

I inspected the native Blender migration supplement source and final raw
before/after reports, saved changed-source hashes, markers and actual exits.
In `gt05-bone-migration-s64-02`, rename changes `bn_head` to
`bn_head_migrated`; delete reduces the bone set from 12 to 11. Both save/reopen
and reach the installed admission check, which rejects exactly
`OBSERVED_NAME_SET` before export/publication. GLB-derived dependencies identify
the shared skin, four body/outfit LOD mesh instances and both clips; socket
ancestry is separately checked and unaffected. Proposed mappings are explicit
but unaccepted. This proves rejection/reporting, not an implemented migration.

Asset evidence establishes 12 meshes, 12 bones, one shared GLB skin referenced
by four skinned mesh instances, 2160 checked skin vertices, 792 sampled poses
and two clips. Separate repeat exports preserve semantics and exact GLB bytes;
the actual crate edit changes geometry/collider/signature. Native same-project
reimport preserves authored script, material override and attachment behavior.
The 27 negative cases mutate copies of native observations; I do not describe
them as 27 native malformed-asset imports.

I visually opened six cardinal views and `idle_15`, `walk_08`, `walk_23` from
the earlier selected fixture capture. I then compared these nine S64 images
byte-for-byte and by SHA256: all are identical to those independently viewed
images. Final strict verification binds all 68 S64 image bytes and their native
metadata. The checker PBR prop, axis markers, avatar/outfit and articulated
poses are visible. Side-view occlusion is real and disclosed. Nine draw calls
per capture are fixture observations, not a production performance benchmark.

Limits retained by this PASS: `public_ack=false`, `editor_activation=false`;
fixed trusted original input and staged immutable assets only. No arbitrary
artist intake, general baking/retargeting, active release replacement, writable
restart, cross-app activation, human usability acceptance, public release,
Linux/Android readiness or HH World acceptance follows. Native symlink
creation was skipped because the host lacks permission. Injected I/O failure
and process death do not certify physical disk exhaustion, antivirus behavior
or power-loss durability. Raw evidence remains local and must be retained;
Git contains source/review bytes and their inventory, not all raw binaries.

This signature applies only to the exact manifest/source/review/raw closure
above. Changes to that closure require a new or explicitly scoped audit.
