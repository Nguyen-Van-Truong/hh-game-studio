ROLE=implementer JSON source bundle; WP=GT-01 reproducible offline bootstrap+rollback; MODEL=grok-4.6 xhigh no-subagents. Owner authorized tools first; GT01 IN_PROGRESS S19. Workspace $WORKSPACE read-only. Coordinator will materialize/test output in isolated copy. No terminal available; never claim tests ran. No plan/lock changes, no global config/PATH, no game/GT02, no commits.
Read studio/toolchain.lock.json and GT-01 plan paragraph. Build two NEW files only: studio/build/bootstrap/install_toolchain.py and studio/tests/bootstrap/test_install_toolchain.py. Return JSON only {schema:"HH3D-SOURCE-FILES-1",files:[{path,content}],rationale,unexecuted_tests,limitations}. Full valid Python3.11 stdlib code, no fences. Keep concise and auditable (about 300 implementation lines, tests separate).
Deliver a real CLI for installing previously downloaded official Godot editor archive into a NEW isolated target root, verifying SHA256+SHA512+size from lock by streaming; read SHA512-SUMS and verify its SHA256 from lock and exactly one matching archive checksum record before extraction. Templates and Blender verification may be separate verify-only commands; do not claim extracted if not implemented. No network download this slice; accept --archive path --sums path --lock path --root path. Clearly label offline acquisition step as pending URL retriever. Validate lock's Godot archive expected fields and console/gui filenames (portable names), digests, version. Do not trust arbitrary filenames, URLs, symlinks/reparse/hardlink paths or conflicting hashes.
Archive staging: reject traversal/absolute paths/drive or ADS/backslash oddities/device names/reparse members, duplicate or case-colliding entries, oversize archive expansions and excessive file count. Only expected console/gui and ordinary license/readme data under bounded allowlist; no extractall. Stream extraction with per-file size cap into a fresh stage under exact root. All target ancestor paths must be checked against Windows reparse and symlinks, no delete/move arbitrary paths. Root is private trusted bootstrap workspace, not a GT02 untrusted safe-open API. Do not claim race-proof handle-based safe-open.
Immutable package directory keyed by pinned version+archive digest (or a unique installation id), manifest of extracted files. Never overwrite existing package/stage or activation pointers blindly. Staging failure keeps prior active pointer and files unchanged; retained failed stages allowed with explicit report.
Activation: minimal durable JSON active pointer under supplied private root, expected previous digest/revision CAS and exclusive create lock, atomic temp+replace. Keep previous pointer in journal for rollback; rollback verifies previous package closure before switching and refuses after intervening activation/manual edit. Do not overwrite newer manual state. Callers must pass expected active revision/hash, or expected NONE for first activation. Crash stages explicitly reported; don't call binary verified unless readback hashes match. No running Godot required in module tests. Coordinator separately launches verified binary later.
Meaningful tests using small synthetic ZIPs/locks/SUMS: initial install+readback, activate second package then rollback, wrong hash/SUMS, corrupt payload, path traversal/case collision/symlink ZIP, duplicate/existing package, edited active state refused, preserve previous active pointer across failed install. Mocks may inject replace failure. Tests runnable directly via python FILE.py, no pytest. Include positive baselines so failure branches are reachable. No delete of user files; tests only tempfile.TemporaryDirectory generated roots.
This is one bounded subdeliverable, not GT01 acceptance. Source/product lock and fixture files are read-only. Honest limitations required.
