One small source change only. You are official grok-4.6 xhigh, no subagents. Workspace $WORKSPACE read only; coordinator applies and executes tests. S19 GT01 in progress. No plan/fixture/global changes. No runtime claims. NO shell tools.
Read studio/build/bootstrap/run_fixture.py function _observed_version and run_process only. Return VALID JSON (not Python triple-quote strings) with {"schema":"HH3D-SOURCE-EDITS-1","edits":[{"path":"studio/build/bootstrap/run_fixture.py","old":<exact old function text>,"new":<new function text>}],"files":[{"path":"studio/tests/bootstrap/test_version_probe.py","content":<full test source>}],"limitations":["unexecuted; coordinator must verify"]}. Source must be escaped JSON strings, newlines as \n. No fences or prose. Do not edit main or any other function.
Goal: _observed_version keeps (Path,int)->tuple[str,str] interface but must reject version spoofing by nonzero exit, dirty stderr, extra stdout lines or suffix garbage. It must use existing run_process to cover Windows Job Object and timeouts (not subprocess.run, not reimplement Windows). Use fresh tempfile.TemporaryDirectory prefix; run_process(argv=[str(godot),"--version"],cwd=godot.parent,output=tempdir,timeout=min(timeout,30),label="version"); require real exit0, wrapper exit0, !timed_out, tree_verified. Require stdout log <=512 bytes and stderr log exactly empty (size0); UTF8 strict; stripped stdout regex fullmatch r"[0-9]+\.[0-9]+\.[0-9]+\.stable\.official\.[0-9a-f]{7,40}". Raise ValueError on failed proof. Return (text,text) on success. Source pin equality remains main/lock follow-up, do not claim solved. Import tempfile locally. Keep temp logs diagnostic-only, ordinary official harness already records version separately.
Meaningful unittest test_version_probe.py load runner via importlib as existing tests do. Mock run_process to create actual stdout/stderr files in supplied output and return real-shaped metadata. Positive exact "4.7.2.stable.official.ed1daf0bf\n" passes. Negative nonzero exit with version-looking stdout, timed_out, tree_verified false, dirty stderr, extra stdout prefix/suffix/multiple lines, >512bytes, invalid UTF8 fail ValueError. Assert mock received --version and timeout capped30, no fake Godot executable actually executed. Existing 5 process tests remain separate and unchanged. Use stdlib unittest runnable directly, no pytest.
Keep implementation <=30 lines and test file <=100 lines. Exact input old function is present; do not guess indentation. Produce JSON source, not a completion report. Do not claim GT01 acceptance.
