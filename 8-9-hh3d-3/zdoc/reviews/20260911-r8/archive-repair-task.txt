grok-4.6 xhigh no-subagents. One GT01 helper repair, no execution claims. Read-only workspace $WORKSPACE with current lock. Previous archive worker result FAILED: sha512 length wrongly64 rather than128, main ignored --sums, hashes/version hardcoded, verify_archive function accepted2args while tests passed3, pytest/unix zip instead of stdlib despite constraints, missing tmp_path variables. REJECTED, never integrated. Fix actual source, not explain intentions.
Output JSON ONLY {"schema":"HH3D-SOURCE-FILES-1","files":[{"path":"studio/build/bootstrap/verify_archive.py","content":<fullsource>},{"path":"studio/tests/bootstrap/test_verify_archive.py","content":<fullsource>}],"limitations":["not executed; extraction/rollback not implemented"]}. Exactly2 new files, no edits to other source. Valid escaped JSON, not Python triple quote. No tool execution available. No pytest. No installation/network. No hardcoded release hashes.
Implement verify_archive(lock_path,archive_path,sums_path) ALL THREE pathlib.Path parameters. Read lock<=64KiB with duplicate JSON key rejection. Require godot.version safe digits.digits.digits-stable; godot.archive dict name/simple portable zip filename, URL exact https://github.com/godotengine/godot-builds/releases/download/<version>/<name> with no query/fragment/userinfo; sha256 regex64hex, sha512 regex128hex, size_bytes type is int NOT bool in1..8GiB; godot.sha512_sums.sha256 regex64hex. For every input and ancestor reject symlink/reparse; file st_nlink==1; use stat identities before/after reading all3 files. All trusted local bootstrap verification, not race-proof GT02 safe-open.
Read SUMS<=1MiB as raw bytes; hash SHA256 must equal lock's godot.sha512_sums.sha256. DecodeUTF8strict, each nonempty/noncomment line matches128hex whitespace optional* filename. Exactly one matching archive.name row and digest==lock.archive.sha512; duplicate matching row even same hash fails, malformed row fails. Archive path.name must equal lock.archive.name. Stream archive chunks1MiB compute sha256/sha512/size; stop if larger than expected, compare all3 at end and before/after stat identities. No writes. Return portable dict schema HH3D-ARCHIVE-VERIFIED-1 artifact/name version size_bytes sha256 sha512 sums_sha256 status VERIFIED_BYTES_ONLY limits. CLI3flags printsJSON/exit0 or terse sanitized failure stderr/exit2; no tracebacks/no absolute username. No extraction.
Tests unittest importlib load helper via Path(__file__).parents[2]/build/bootstrap/verify_archive.py. Real tempfile fixture creates small synthetic ZIP using zipfile.ZipFile, then computes sha256/sha512/size, writes valid official-formSUMS and lock. Don't use real production digests on synthetic ZIP. Positive verify returns observed, verifies files unchanged; negatives corruptZIP,wrongsize,badSUMS hash,duplicate matchingSUMS (update its pinned SHA256 so duplicate branch actually reached),missing row, malformed line,boolean size,invalid digest,wrongURL,hardlink on supportedOS. Each baseline valid before mutation, no unrelated failure masked. One class setUp tempfile with cleanup; unittest.main. Under150lines module+150tests. Actual tests coordinator will run; don't invent PASS.
