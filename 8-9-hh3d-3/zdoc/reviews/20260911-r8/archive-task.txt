ROLE=bounded implementer, GT01 archive verification helper only. grok-4.6 xhigh no-subagents. Read-only workspace $WORKSPACE. Owner tools-first authorization, current S20 IN_PROGRESS. No model fallback, no global config/PATH, no downloads, no install/extract or activation. Not related to critic job; this is new GT01 implementation scope. No shell execution available; coordinator materializes and tests source. Never claim tests ran.
Read studio/toolchain.lock.json and existing test_gt01.py to understand data shapes, not full plans. Allowed NEW files only:
studio/build/bootstrap/verify_archive.py
studio/tests/bootstrap/test_verify_archive.py
Return ONE valid JSON object ONLY {"schema":"HH3D-SOURCE-FILES-1","files":[{"path":"...","content":"..."},{"path":"...","content":"..."}],"limitations":["Tests not executed by worker; extraction/acquisition/rollback pending"]}. JSON strings escaped correctly (\n), no Python triple quotes or extra commas/prose/markdown fences. Keep module<=150lines and tests<=150lines. Do not modify existing runner/tests/lock/plans.
Implement Python3.11 stdlib CLI --lock --archive --sums. Public verify_archive(lock_path:Path,archive_path:Path,sums_path:Path)->dict reads portable lock['godot']['archive'] fields name/url/sha256/sha512/size_bytes and godot.sha512_sums.sha256. Do NOT hardcode current digest/version; trust reviewed input lock but validate its fields/types strictly. Archive filename must match name, portable simple .zip name only; URL HTTPS exact hostname github.com and path /godotengine/godot-builds/releases/download/{lock.godot.version}/{name}, no query/fragment/userinfo. Version safe [0-9]+.[0-9]+.[0-9]+-stable; no arbitrary schema keys accepted silently if needed but don't reject unrelated legitimate top-level lock metadata.
Stream archive once in bounded chunks1MiB to compute SHA256+SHA512 and byte count. Verify exact expected size/sha before return; size limit reasonable<=8GiB not read_bytes/read all. SUMS file bounded<=1MiB and strict UTF8, compute pinned SHA256, parse official sha512sum format 128hex + whitespace + optional '*' + filename. Exactly one entry for archive.name; duplicate entry rejects even equal digest, malformed line rejects except blank/comment. Required entry SHA512 equals lock+observed. Lock<=64KiB. Reject duplicate JSON keys/invalid digest/booleans as size/missing fields; negative/oversized size rejects. Check existing file and all original path ancestors for symlink/reparse, hardlinks (st_nlink !=1). No mutation anywhere. Record pre/post file stat identities (dev,ino,size,mtime_ns) for archive/SUMS/lock; fail if changed while reading. Trusted local verification only, not GT02 adversarial safe-open; no security claim beyond implemented checks.
Return portable report {schema:'HH3D-ARCHIVE-VERIFIED-1',artifact:name,version,size_bytes,sha256,sha512,sums_sha256,status:'VERIFIED_BYTES_ONLY',limits:[no install/runtime acceptance]}. CLI stdout JSON report, actual success exit0, expected error print concise stderr exit2 without traceback/absolute username. Do not print local paths. No fake network or cryptographic signature proof, no H2 acceptance.
Meaningful unittest direct entry (no pytest): tempfile root with valid small synthetic ZIP and matching synthetic lock/checksum corpus. Must work independent actual release ZIP; URL uses synthetic archive name/version pattern and hashes supplied by fixture. Tests positive streaming digests, corrupt archive, wrong size, sums mismatch, missing/duplicate checksum rows, invalid metadata/digests/URL, hardlink or symlink if permitted with explicit skip, and after-read identity changed injection. Assert no files changed on verify by content hashes. Tests use importlib to load helper relative __file__. Keep expected checksum source truthful; compute synthetic ZIP first then metadata. No global filesystem scans or engine launches. End after concrete files.
