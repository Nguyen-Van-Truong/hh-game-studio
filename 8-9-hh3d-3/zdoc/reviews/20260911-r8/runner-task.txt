ROLE=implementer JSON source bundle; WP=GT-01 runner admission; MODEL=grok-4.6 xhigh no-subagents. Authorization: owner implementation, current S19. Dependencies GT01 OWNER_START only; GT02 not open. Work in $WORKSPACE read-only; coordinator materializes your bundle in another isolated copy. No canonical edits, no commits, no runtime claims. Your read tools work; shell is unavailable. Do not pretend to execute.
Read input-manifest.json and studio/build/bootstrap/run_fixture.py, studio/tests/bootstrap/test_run_fixture.py, studio/toolchain.lock.json. Read tools plan GT-01 paragraph if needed, not all historical plans. Existing fixture functional evidence is valid; do not redesign it. Preserve the existing gated Windows Job Object run_process mechanism and its interface.
Return JSON ONLY with schema "HH3D-SOURCE-EDITS-1", edits:[{path,old,new}], files:[{path,content}], rationale, unexecuted_tests:[commands], limitations. Every old block must match input exactly once. At most 2 edited/new files total. New test file can be studio/tests/bootstrap/test_runner_admission.py. Do not return full unchanged runner. No markdown fences. No accepted signatures.
Concrete bugs to repair in run_fixture.py main admission/result pipeline:
1) It trusts caller console/gui/version independently of lock; validate these against studio/toolchain.lock.json godot fields before any binary spawn. observed version must fully match lock.observed_version (including source commit), subprocess version exit must be zero and stderr clean. Reject absent/malformed lock, missing GUI, wrong commit, nonzero version exit, stdout extra garbage. Avoid unbounded version subprocess: route via existing run_process or explicit bounded valid proof, keep backward interfaces tests.
2) It resolves away reparse roots before checking. Check original supplied paths and every existing ancestor before resolve for studio/godot/GUI/output; reject symlinks/junction, hardlink executables, unsafe run/command IDs and output equal/inside source or reserved/existing evidence. Do not claim this replaces GT02 adversarial OS safe-open (GT01 trusted bootstrap only).
3) Compare actual copied fixture closure against pre-copy manifest to detect corrupt copy; recheck whole source and binary digests after runtime. Source changing during runs must reject candidate.
4) A stdout WARNING/ERROR on any successful run currently escapes stderr-only check. Reject unexplained warning/error in either stream, require exactly 3 completed runtime phases, each real exit0, no timeout, tree_verified and one valid PASS/QUITTING trace. Preserve phase data in diagnostic output. Every failure after output reservation should yield evidence/diagnostic where possible, not a success banner.
5) Add meaningful unittest tests with valid baseline fixture+lock, fake run_process/version seams (never execute random binaries), wrong versions/digests, duplicate output, source change during copy/runtime, trace missing/duplicate, warning stdout. Prove a valid baseline reaches each reject branch; a setup that fails earlier is not coverage. Include one positive baseline. Native process behavior remains separate from mocks.
Keep patch under ~300 changed implementation lines. You may add a helper to keep main coherent. Deliver actual concrete edits, not advice. Preserve flags or clearly document justified interface differences. No source changes beyond the exact paths above. Source manifest in workspace is the base hash authority; S19 plans remain read-only and IN_PROGRESS.
