ROLE=bounded repair. grok-4.6 xhigh no-subagents. Workspace $WORKSPACE contains original canonical source. Also read-only reference failed candidate at C:/Users/truon/AppData/Local/Temp/hh3d-r8-version-check-20260910T231711Z/version-check/coordinator-stage/studio/{build/bootstrap/run_fixture.py,tests/bootstrap/test_version_probe.py}.
Return JSON ONLY {"schema":"HH3D-SOURCE-EDITS-1","edits":[{"path":"studio/build/bootstrap/run_fixture.py","old":<exact original _observed_version function text from workspace>,"new":<fixed function>}],"files":[{"path":"studio/tests/bootstrap/test_version_probe.py","content":<fixed test file>}],"limitations":["not executed by worker"]}. Escape JSON strings, NO Python triple quotes, NO markdown fences. No tool execution available, no fabricated run results, no source writes. Coordinator validates and executes. Read original function before forming old block.
Prior candidate failed 7/10 tests for obvious bugs. Fix exactly these:
- tempdir/r['stdout'].read_text calls read_text on str. Correct (tempdir / r['stdout']).read_bytes(), same for stderr.
- Required regex is [0-9]+\.[0-9]+\.[0-9]+\.stable\.official\.[0-9a-f]{7,40}. Candidate omitted .stable.official. Use re.fullmatch and ASCII numeric classes.
- Before reading stdout, enforce file size <=512 bytes and stderr stat size ==0. Decode strict UTF8 and strip stdout; return (s,s). This function uses existing run_process with its gated Windows Job/process proof.
- Require r.get('exit_code') == 0, r.get('wrapper_exit_code') == 0, r.get('timed_out') is False, r.get('tree_verified') is True. Missing fields reject. ValueError diagnostic on bad proof/log/version; invalid UTF8 reject ValueError.
- Mock tests currently write files to DIFFERENT TemporaryDirectory than implementation. Use side_effect(argv,*,cwd,output,timeout,label) that writes actual logs under the supplied output argument and returns metadata. Do not mock TemporaryDirectory. Capture call args using Mock.side_effect.
- Fake godot Path('C:/fake/Godot_console.exe') str is platform-specific; assertions compare str(fake) rather than hand-coded slashes.
Implementation only _observed_version, local import tempfile, TemporaryDirectory(prefix='hh3d-version-') and run_process([str(godot),'--version'],cwd=godot.parent,output=Path(td),timeout=min(timeout,30),label='version'). No unrelated main/lock/process function changes. Keep <=30 implementation lines, <=100 test lines.
Meaningful tests positive exact4.7.2.stable.official.ed1daf0bf newline passes and returns stripped tuple; negative nonzero target exit, nonzero wrapper exit, timeout, missing proof, tree false, dirty stderr incl whitespace, prefix/suffix/multiline, >512bytes, invalidUTF8 each fail; assert invocation timeout capped30 and --version. Native tests are independent. Use unittest and actual temporary log files; no fake executable run. This is GT01 partial, not acceptance.
