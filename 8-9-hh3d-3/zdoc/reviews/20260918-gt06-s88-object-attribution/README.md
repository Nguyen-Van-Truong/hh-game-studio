# GT06 S88 bounded ObjectDB attribution diagnostic

`AUTHORITY=0`; diagnostic only. This disposable overlay is copied into a private project and never modifies the 51-file runtime closure or acceptance thresholds. It runs the existing full host/native sequence with the unchanged source/profile and emits compact identity/class summaries at batch 4 and only when ObjectDB exceeds that baseline. It is intended to distinguish identity churn from retained runtime objects after S86 failed at batch 17 (`71128 → 71130`).

The run is not eligible for the GT06 dataset, does not relax `CAMPAIGN_RETAINED_COUNTER_GROWTH`, and must have actual target/helper exits, Job zero/closed, released handles and terminal receipts before interpreting a no-growth result. If it reproduces growth, preserve the identity snapshot and stop for diagnosis; do not launch a full campaign blindly.
