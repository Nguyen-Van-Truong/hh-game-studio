# GT-02 S46 — independent acceptance critic B

VERDICT=PASS
TICK=yes
SOURCE_CLOSURE_SHA256=f28056d8ff705a60d48a54f9dee80f16554494fa976bd0bfabee84bb51fecbbf
SOURCE_FILES=106
REVIEWER_ROLE=INDEPENDENT_READ_ONLY_ACCEPTANCE_CRITIC_B
SOURCE_AUTHORSHIP=NONE

Reviewed on 2026-09-16, Asia/Saigon. This vote applies to the exact frozen closure above and the GT-02 baseline in the current tools plan, including section 2.2, TQ02 and TX11/14/15. It authorizes coordinator consideration of this candidate; it does not tick the plan, accept another closure, or claim later GT work. No acceptance-blocking defect was established in the reviewed scope.

The checkout baseline when review began was `1951d802fd570a435198900bd5ea382c2e00b179` plus the frozen S46 working files. The working source itself was rehashed; the prior commit and prior critic signatures were not substituted for current evidence. Source, tests, product documentation and plans were read-only throughout this review. Only this report and `20260916-gt02-s46-critic-b-evidence/` were written. No Vault Fighters/legacy mutation, root authoritative checker, plan tick or commit was performed.

## Evidence binding and actual outcomes

`20260916-gt02-s46-critic-b-evidence/integrity.json` records an independent complete source inventory, per-file byte hashes, canonical closure reconstruction, all nine candidate artifact hashes, raw unittest result/host consistency, and all three native manifests with exact path-to-hash equality. The evidence parser also rejects duplicate JSON keys. It found no missing or extra source file in the declared runtime/test/build/fixture closure.

The coordinator verifier source was read and its read-only `verify()` function was run as an additional check. Its main entry point was not run, so this review did not modify coordinator evidence. The review separately checked source and artifact inventory and raw process/test records rather than relying on the verifier's banner or saved PASS field.

`20260916-gt02-s46-audit/git-byte-verification-index.json` reports the staged source/evidence byte check. This critic independently read all 106 source blobs and 138 listed evidence blobs from the current Git index using `git cat-file --batch`, compared their exact bytes with the reviewed files, and passed. That proves the reviewed bytes are staged; it is not a claim that a new commit already exists. The critic report/evidence remains a separate output for coordinator staging.

| Package | Observed result |
| --- | --- |
| `20260916-gt02-s46-01` | Protocol 417 executed: 413 pass, 4 explicit environment/platform skips; bootstrap 56/56. Actual target and wrapper exits 0, no timeout, owned process trees verified empty. Raw summaries equal candidate summaries; all nine artifacts match their hashes. |
| Candidate golden artifacts | 2,396 rows per Python/Node/Godot consumer agree. Godot source remains unchanged, exit is captured, stderr is empty, and before/after leftover lists are empty. |
| `20260916-gt02-s46-native/run-01` | Three actual AppContainer cases through `ManagedPipeService`: create; owner reopen by storage ID then replace/retry/Stop; lost work reply with surviving control lookup/Stop. Native child exits 86, outer target/wrapper exits 0, each child Job is empty. Actual selected-file identity/hash and event/custody observations agree. No direct launcher dispatch/advance supplies the claimed mutation. |
| `20260916-gt02-s46-boundary/run-01` | Twenty native replacements with changed FileIDs during 306 denied adversarial attempts, zero unexpected/allowed attempts, scratch positive control. Native child exit 67, Job/PIDs empty, outer exits 0. |
| `20260916-gt02-s46-registry-native/run-01` | 36/36 denied attempts, 12 distinct broker custody updates, same-prefix/access positive control successfully read/written by the child and read back by the host. Native child exit 89, Job/PIDs empty, outer exits 0. |

The old S44/S45 marker names in the last two diagnostic harnesses do not identify the runtime source: their actual runtime maps contain all 106 current files and reconstruct the exact S46 closure. Diagnostic Python/C sources and compiler/link outcomes were also verified. Native runs report profile/mapping/temp removal; managed evidence reports zero remaining endpoint/I/O/event owners. The managed native restart is owner reopen within its broker process. Actual broker-process crash/restart is separately exercised by subprocess tests; these are distinct claims.

The four full-suite skips are three symbolic-link creation cases unavailable to this Windows account and the non-Windows negative-capability case. Windows junction/reparse/native boundary coverage ran. These skips do not prove Linux support or successful creation of privileged Windows symbolic links.

## Durable custody, retry and fencing

Reviewed `private_events.py`, `custody.py`, `custody_registry.py`, `managed_fixture.py`, `fixture_selector.py`, the file-consumer/rearm path in `safe_replace.py`, and their relevant tests.

- An event append writes and flushes the stream, validates the complete chain/readback/EOF, then persists the exact high-water binding in Registry before returning. Registry store performs expected-value comparison, native set, flush, readback and security/identity revalidation. Failed post-write barriers poison the owner and retain uncertainty.
- Reopen uses the protected Registry storage ID and typed root/stream identities. Whole-record truncation below the saved high-water is rejected; a complete suffix is semantically reduced and cannot cause blind re-execution. A custody failure after terminal append remains UNKNOWN in the failing owner; restart confirms the existing file and terminal receipt without replacing those bytes again.
- Normal admission validates the immutable command digest and checks existing receipts before fresh-write preflight. The same ID/hash returns the existing receipt after lease/session replacement; changed content conflicts. Terminal results remain in durable event records, with bounded in-memory indexing and fail-closed capacity reservations. The separate general journal's retention/tombstone and process-CAS tests pass in the full candidate.
- A terminal restart first reacquires native ownership, checks the complete history and exact active file, mints a new lease ID/fencing epoch, and rearms only the verified fixed consumer namespace. Old unexpired lease fields cannot authorize a new command. A second owner cannot advance custody while the native writer guard is held.
- Pending, stopped, orphaned and ambiguous roots retain their hold. Broker construction does not reconstruct inherited pending jobs, and duplicate lookup cannot schedule one. Verified-empty restart remains explicitly held. Broader automatic recovery is outside this GT-02 acceptance and remains GT-07 work.

## Lifecycle, Stop/Cancel and UNKNOWN

Reviewed `managed_service.py`, `selector_pipe.py`, `pipe_endpoint.py`, `pipe_io.py`, lifecycle tests and the actual managed-native driver path.

- One service owns the broker and both role-bound endpoints while borrowing durable storage. Owner/broker closure refuses to destroy storage beneath an attached service. Partial start, timed-out join, failed native close and failed broker cleanup retain the original cleanup owner and chain for retry.
- The job wake is published before response I/O. Losing ACCEPTED_PENDING delivery therefore cannot lose admitted-work visibility. Work failure holds new admission and future phases while the separate healthy control reader remains usable. Control failure revokes the entire logical session, including rotations, and signals both endpoints.
- The pre-freeze service-deadline defect is repaired in this closure by a separate watcher and post-read/pre-dispatch deadline checks. A blocked phase pump does not prevent admission hold, token invalidation or endpoint cancellation. Already-admitted synchronous I/O may finish; the following phase cannot start automatically.
- Stop signals before waiting on managed storage registration; the selector rechecks Stop after potentially slow quota/pin work before the next effect. Pre-selection cancellation persists CANCELED with truthful staging/selection flags. Selected or uncertain work remains UNKNOWN and requires lookup/reconciliation. Durable Stop survives restart and reconnect.
- `close()` uses a shared drain deadline and verifies thread liveness. Incomplete overlapped I/O retains buffer/OVERLAPPED/event/handle ownership until observed completion; cancellation alone is not treated as completion. The enclosing owned Job remains the final bound for native calls that cannot return. This is not a hard real-time guarantee for arbitrary kernel/driver stalls.

The original pre-fix FAIL remains in `20260916-gt02-s46-service-design-review.md` and `20260916-gt02-s46-service-review-evidence/run-01`, against closure `c8bfc4a717d2f07570c5758194e6b7fd2cff522a05f8070538f87523e107191a`. This acceptance does not relabel that diagnostic as PASS.

## Independent focused execution

`20260916-gt02-s46-critic-b-evidence/run-01` ran an immutable copy of all 106 frozen files under the accepted owned Job runner. Nineteen focused tests passed, zero skips/errors/failures, in 8.159 seconds; actual target PID 42452 exit 0, wrapper PID 42676 exit 0, no timeout, process tree verified clean. Source and snapshot hashes remained unchanged. The host-enforced limit was 40 seconds.

Eighteen selected existing tests independently re-exercised terminal/Stop/pending restart, lost high-water record, custody barrier failure, real process exit 87 then protected-storage-ID recovery, stale fencing, orphan preservation, second-owner exclusion, lost pending reply, rotation revocation, and retryable drain/start/close failures.

The nineteenth test was written specifically for this review. It blocks an already admitted stage, rotates the credential, waits across the 900 ms service deadline, attempts a new lease on the waiting work lane, and verifies no new lease/event, rejection of the rotated credential, no selection/adoption, and zero threads after close. Releasing the already-admitted stage finishes it once but cannot advance to selection. Together with the focused post-read check, this directly addresses both parts of the retained deadline failure.

Reproduce with a new output directory/run ID by adapting the retained `run_review.py`; its immutable `run-01` intentionally refuses overwrite. `audit.py` performs read-only evidence verification and writes only this critic's `integrity.json`.

## Acceptance limits

This is the authenticated, bounded, inert `fixture.active-release` scope with actual managed publication and readback. It does not advertise arbitrary-path write/atomic replace, Godot or Blender mutation, public recovery controls, Linux/Android conformance, general client/model conformance, or power-loss guarantees. General safe-write flags remain false. GT-03 through GT-10 retain their planned gates; no human, legal, signing or release acceptance is supplied here.

Final finding count: 0 acceptance-blocking findings for this exact closure and declared GT-02 baseline. PASS / TICK=yes.
