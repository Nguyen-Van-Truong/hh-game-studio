# S83 GT-06 requirement mapping preparation

AUTHORITY=0. PREPARATION_ONLY=1. FORMAL_ACCEPTANCE=false. This is neither a
critic review nor the final evidence seal. GT-01–05 remain accepted; GT-06 is
open. GT-07–10 are not opened by these documents.

The current source target is checkpoint `b3862a10`, the 51-file benchmark
closure `e010180a1bf85b7f565e9aa39734f8551caf170a85b36c50598be556b723ce7f`,
with profile
`0cd5b53055853b592d4376a6b8a89d69c70ea5facc40dfc25ebd6fcd6dbf4d85`.
These are the identities declared by plan S83 and S81 `repair-source.json`;
this preparation does not independently rehash the runtime or raw packages.
Observed repository HEAD was `c1726579759bd1d13799cb2365bf75079b03c3a7`.
The scoped Git status for `studio/` was clean when read. A later evidence-only
HEAD is not a new runtime source identity.

- `requirements.md` and `requirements.json` contain all F01–F15, explicit
  tests/artifacts, lane provenance, reuse limits and remaining actions.
- `seal-checklist.md` describes the future terminal-only sealing operation for
  the current 51-file closure and child cleanup schema. It is not executable
  acceptance or an instruction to seal the failed S81 campaign as successful.

Preparation used bounded metadata/document/source reads. No engine, test,
benchmark import, process control, commit, plan edit, runtime edit or large/raw
package rehash was performed. Only files in this directory were authored.
The active diagnostic's raw tree was not read. Its current liveness/result
must be obtained by the coordinator, not inferred from this snapshot.

## Reuse decision

Keep the seven S79 service/reviewer runs with their original run IDs, 174-file
backend map and five-file GUI map; keep S69 managed replay's 159-file map and
four verifier pins. S81 `dependency-impact.md` records exact path/digest and
collector-membership joins after accepted transport restoration. This is a
dependency-exact basis for reuse, **not** a claim that S79's broad 453-file
freeze, S69's whole test snapshot, or old logs have the current benchmark hash.

S65 fault/repair/perf remains historical with its original causal anchors.
S65's native154 map differs in fixture/observer files; its three perf collector
dependencies match according to the S80 bridge. S68 integrated GUI maps differ
in service/journal paths and omit the new disk index; S79 supplies current
integrated service/GUI proof. U69 service cases use the U79/S81 supplement.

New benchmark transport, campaign cleanup and complete measured-workload proof
cannot inherit old benchmark outcomes. S81 checks02, HTTP02 and cleanup smoke
are useful scoped supplements. S81 campaign01 failed at batch15, ObjectDB
71128→71130, with zero complete runs; its 16 captures never become accepted
samples. S82 attribution/native-only diagnostics have independent overlays,
instrumentation and durations and cannot enter the acceptance dataset.

## Actual remaining work

1. Resolve the measured failure without relaxing the fixed profile; freeze any
   affected source and obtain ten complete fresh process pairs ×35 batches with
   all performance, effect, retained-counter, ownership and terminal proofs.
2. Join every F01–F15 row to exact raw bytes, source/test/driver/verifier/schema
   dependencies and the accepted asset→Play→repair/replay chain; create a final
   reproducible manifest. Retain all failed prefixes and evidence limitations.
3. Obtain two new independent `PASS`/`TICK=yes` reports on that same final
   manifest hash, then coordinator acceptance. No critic report/hash or
   acceptance decision exists in this preparation.

No new native functional orphan was identified in this bounded metadata
mapping. That statement is not acceptance and does not prevent final critics
from identifying a real gap. Multi-project fairness/recovery belongs GT-07;
physical Android belongs GT-08, conformance GT-09 and packaging GT-10. The
existing shared fault limits are retained rather than expanded into new gates.
