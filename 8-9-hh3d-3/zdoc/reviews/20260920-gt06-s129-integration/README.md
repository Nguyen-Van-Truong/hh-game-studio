# S129 integration — candidate diagnostic, not acceptance

Runtime checkpoint d97960f3 changes only host/core/transport.py within the
53-file execution closure: request threads are weakly registered before start
and both listeners drain during close. A real prepatch two-listener request
outlived close; after repair 61 transport tests and 166 affected benchmark tests
pass. This is a demonstrated shutdown defect, not a proven S127 counter cause.
Profile, native workload, strict counter gates and timeout values stay pinned.

The diagnostic answers whether a future original retained-handle gate has
persistent or transient host/editor kernel-object identities after rejection,
with corrected production PSS schema and independently retained launcher exit.
S128 cannot be resumed: its host/editor ended without terminal receipts and its
helper contract was defective. All S128 raw and its UNKNOWN disposition remain.
No partial data are imported; the new ID is gt06-s129-host-attribution-02.

post_failure_full.py retains the stock35 batches and original screen_sample,
allows natural full drain, and observes identities only after an original gate
rejects. Failed observation is retained separately and never changes rejection.
The integration's two no-engine tests prove cleanup_owner is adopted before an
optional Job-readback write can fail, plus finish/verify/close ordering using a
synthetic owner. Synthetic receipts are not actual engine exits. Raw test output
and hashes are in launcher-tests-02. Helper26 tests and passive observer actual
0/7/47/timeout probes are in their sibling packets. None is a final critic.

Inner wall7410s, outer7530s, passive observer7770s and scheduled bound7800s.
Initial estimate45–80minutes from prior stock runs; update from real completed
batch durations. No ETA for the entire plan. Keep source/helper/profile and
workstation fixed; no concurrent engine or heavy tests. If healthy, inspect at
10-minute heartbeat only. Read actual target/helper exits, Job zero/closed,
handles, Stop, drift and all attempts at terminal; scheduler0 alone is not PASS.

Changed transport invalidates old exact-dependency reuse for affected lanes;
61+166 are candidate checks only. Before final GT06, revalidate affected accepted
functional dependencies, prove10freshpairs×35 and freeze full requirement/test/
evidence closure for two independent critics. GT07–10 stay unopened; GT08 needs
a physical Android device. The tools are bounded authoring operations, not a
finished Superagent/HH World game. No gates were removed or loosened.

History: archive/s128-plan.snapshot is byte-exact AUTHORITY0. Historical failed
runs keep their raw/portable hash domains. Fixture journals in host-quiescence
stay local; retention.json identifies committed files and local-only hashes.
