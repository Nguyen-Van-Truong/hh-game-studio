# GT-02 S41 independent critic B — S39-02

- **Reviewed closure:** `2fb0ec013c4037c844b38f7be221c0aa7a6bd905492fa7c4a53eaa8e474c9d59`
- **Candidate:** `GT02-S39-20260916-02` / `cmd.GT02-S39-20260916-02`
- **Scope:** read-only review of the GT-02 plan/DoD, S39-02 candidate and audit manifests, protocol/selector/pipe/journal implementation and tests, focused RPC evidence, and native AppContainer evidence. I did not edit source or run the full suite.
- **Verdict:** `FAIL` (evidence is not completely bound to the requested frozen closure)
- **TICK:** `no`

## Evidence that is valid on the reviewed closure

The S39-02 candidate manifest is internally consistent: `source-closure.json` and `candidate.json` both bind `2fb0ec...`; the source tree currently reproduces that digest and 84-file inventory. The protocol lane reports 283 tests (279 pass, 4 explicit platform skips), with host/wrapper/child exit 0, clean owned process tree, and no failures/errors. Bootstrap reports 56/56 with exit 0. The focused substituted-frame selector run is 16/16 with a real owned host exit and no skips. The protocol tests cover admission, duplicate/retry after lost reply, phase crash/uncertainty, Stop/Cancel ordering, session/token/fence checks, private event durability barriers, pipe cancellation/close ownership, and journal CAS/retention behavior.

The native record also contains useful observations: seven cases, actual child exit 61 in each case, Job active process/PID lists empty at close, and `ERROR_ACCESS_DENIED (5)` for pipe ACL/owner/second-instance/private-root/.events probes. It explicitly labels the consumer as the in-memory fixture and `safe_write`/`atomic_replace` as unsupported, so those limitations are not misread as production adapter proof.

## Blocking finding

**P1 — native AppContainer evidence is not bound to the exact frozen source closure.** The requested candidate closure lists:

```
host/core/FIXTURE_SELECTOR.md = 00efdc0e3d585ee676e39fa57fb130f9b41d8ee5b495d1b101e9deada206c698
source_closure_sha256       = 2fb0ec013c4037c844b38f7be221c0aa7a6bd905492fa7c4a53eaa8e474c9d59
```

But `zdoc/reviews/20260916-gt02-s39-native/run-01.stdout.json` records a different 84-file runtime closure:

```
host/core/FIXTURE_SELECTOR.md = 0d44bd1fe3ea5bfe147a1bf2bf239bb0c74b7a8e89ab9bbad5ab79a87a880009
runtime source_closure_sha256 = 6972f55fbc72e3095ef4869c69428b89c5aea68ca24532a96d174dbc2c33131f
```

The comparison has the same 84 paths and this is the only differing file, but exact source identity is still false. The native run is timestamped 10:36:35Z while S39-02 candidate capture starts at 10:41:41Z. Therefore the native 7/7 endpoint boundary cannot be credited as proof for closure `2fb0ec...` until reminted against that closure. `verify_evidence.py` checks native case markers and exits but never compares `raw['runtime_closure']['source_closure_sha256']` (or its per-file hashes) with the candidate closure; this is the checker omission that allowed the stale binding.

## Explicit limitations (not additional defects)

The plan itself keeps GT-02 at CANDIDATE because Windows public safe-write/atomic-replace is intentionally disabled (`GT02_SAFE_WRITE_STATUS=GAP_UNSUPPORTED_WINDOWS`; `safe_write=false`, `atomic_replace=false`). Private staging and AppContainer denial do not establish a public project-root write primitive. The native client is a fixed fixture and does not prove a Godot/Blender consumer, external source-revision scanner, namespace activation, or physical power-loss custody. Four protocol skips are environment capability skips (symlink creation/negative Linux-only); Windows junction/TOCTOU coverage is present. These are declared boundaries, not bugs to relabel as PASS.

## Smallest actionable fixes

1. Freeze closure `2fb0ec...` and remint the native AppContainer run with a new run/command identifier. Require its recorded runtime closure digest and every file hash to equal `source-closure.json`; update the diagnostic manifest and evidence hashes after that run.
2. Add a verifier assertion joining native `runtime_closure` to the candidate closure (digest and full path→hash map), and fail closed on missing linkage. Re-run the audit only after this assertion passes.
3. Keep `safe_write`/`atomic_replace` and formal GT-02 acceptance closed until the separate Windows capability proof exists; do not convert the native denial fixture into that proof.

**Final disposition:** selector/pipe/journal protocol behavior is well covered on the candidate closure, but the native endpoint evidence is stale by exact-closure identity and the checker does not detect it. `TICK=no` for this critic.
