GT-02 S30 — independent frozen-source critic A

VERDICT=FAIL
TICK=no
SUPPORTED_FIXTURE_CHECKS=PASS
SOURCE_CLOSURE_SHA256=60799065f406fbb3b13d2dc0df093477210927f86e84b54175dfd36351521a1a
SOURCE_FILES=62
CANDIDATE_RUN_ID=GT02-S30-20260915-01
CANDIDATE_COMMAND_ID=cmd.GT02-S30-20260915-01
CANDIDATE=20260915-gt02-s30-01/candidate.json
REVIEWED_UTC=2026-09-15T16:05:25Z
BASELINE_HEAD=642d6f51158054a40862497c92d771512e8729d0

The journal/transport and candidate artifact checks passed in the supported
fixture scope. GT-02 cannot receive TICK=yes under its unchanged requirements:
TX15 expressly makes unavailable safe-open a GAP, and this source still denies
every Windows file mutation. This verdict does not treat safe refusal as a bug,
and does not permit widening capabilities to obtain acceptance.

This was a read-only independent review of source/tests, the scoped AGENTS.md,
plan section 2.2, GT-02, TQ02 and its TX requirements, and the S30 candidate.
I authored no implementation or candidate tests. I read no other S30 critic
verdict, preparatory audit report, or coordinator verification report. Writes
were restricted to this report and adjacent 20260915-gt02-s30-critic-a/ evidence.
No Godot, Blender, full-suite runner, shared fixture, plan update, commit, tick,
or additional agent was started by this critic.

Prioritized finding:

- [P1, acceptance GAP] Required safe-open mutation capability is unavailable.
  Plan section 2.2 requires protection at use/open/replace, and TX15 states
  "Safe-open unavailable là GAP." GT-02 explicitly references TX15.
  studio/host/core/safe_open.py:156 advertises safe_open=false,
  safe_write=false and atomic_replace=false. The root-probed instance at
  :182 advertises read_identity=true but atomic_create_new=false and both
  write/replace=false. All four mutation entry points at :236–246 immediately
  raise UNSUPPORTED_SAFE_OPEN_WINDOWS on this host. The S30 candidate explicitly
  records the same gap. Held-handle read/identity, negative junction/hardlink
  cases and denial before I/O do not establish safe create/write/publish under
  a swap. Keep GT-02 unaccepted and GT-03/04 behind the dependency until the
  existing gate is met; this review does not rewrite that gate.

No additional journal/transport correctness defect was reproduced in this
review. The following results are positive evidence, not a second acceptance
verdict:

- Independently enumerated the complete declared local source/test/fixture
  closure, checked all 62 SHA256 values against current bytes, and recomputed
  the sorted product-path/NUL/digest/newline closure hash. There were no missing
  or additional files in the covered directories. All 62 files also matched
  the bytes Git's current clean filters would store, checked without staging.
  The hash was unchanged after the focused replay.
- Independently checked all nine bound artifacts and the artifact inventory.
  Both host JSON records matched the candidate target PIDs and exit codes.
  Their timestamps fall inside the candidate run; both wrapper and target
  exits are 0, with the captured Job Object tree result verified and no timeout.
  Separate unittest completion markers, unique test IDs and stderr completion
  lines agree: protocol 154 executed, 150 pass, four explicit platform skips;
  bootstrap 56 executed, 56 pass, no skips. I inspected the runner's ownership
  and exit-capture implementation; I did not substitute a PASS banner for exit.
- Reconstructed the pure-Python expected vectors and compared all 2,396 exact
  canonical strings, UTF-8 hex values and SHA256 values to both independent
  consumer artifacts. Godot has nine explicit negative rows, exit 0, empty
  stderr, no warning/error/leak diagnostic and no leftover process in its
  captured before/after observations. Its script hashes match the closure;
  its generated vectors.json and project.godot hashes were reproduced from
  frozen inputs in memory. Node's consumer-source hash and both consumers'
  test-source hashes also match. The Godot binary hashes agree with the lock.
- Ran ten selected regression tests in disposable temporary fixtures, with
  captured process exit 0: five journal durability tests; the six-fault history
  availability matrix across lookup/retry/cancel/archive; and the four suites
  cutting real socket connections around PENDING, COMMITTED lookup, Cancel
  and Stop. The selected run completed 10/10 in 11.042 seconds without skips.

Durability audit:

studio/host/core/journal.py:263 records whether a JournalError arose before
history was readable or after the guarded operation. Reload at :289 opens the
journal with write access and requires flush/fsync before returning any stored
receipt, including to a new Journal/process. Append at :399 retains configured
terminal capacity before admitting a pending command. Transport at :507–518
maps an uncertain phase or integrity/durability failure to UNKNOWN, closes
admission and supplies lookup.reconcile rather than no_effect.

The five focused I/O cases confirm the distinction. Failed pre-append reload
or a write failing before terminal bytes preserves durable PENDING; the host
reports UNKNOWN and does not execute the ID again. A partial terminal record
remains JOURNAL_TRUNCATED even when fsync recovers. Complete terminal bytes left
by flush/fsync failure cannot authorize success while the barrier continues
failing; after a successful barrier, original COMMITTED may be replayed.
Fresh interpreters, archive/compaction and same-ID retry are covered, with one
fixture effect throughout. These are process-level injected I/O observations,
not claims of a physical power-loss/storage-controller experiment.

The availability matrix also preserves UNKNOWN when lock contention, an invalid
guard, a guard hardlink, configured byte/record limits during reload, or damaged
legacy lock metadata makes history unavailable. It checks all four response
routes, the requested command ID, absence of no_effect and unchanged journal
bytes; after recovery the original committed receipt is replayed without a
second effect. Configured pre-admission capacity rejection remains distinct.

Canonicalization/admission audit:

Digest construction covers operation, target, payload and schema, with dedupe
scoped by project and command ID. Lease changes do not manufacture a fresh ID.
Wire parsing rejects duplicate members, invalid UTF-8/Unicode, non-finite values,
unsafe integers and bounded-shape violations. Payload canonicalization preserves
Unicode code points; NFC name validation is a separate domain rejection.
The host only dispatches the fixed fixture operations, checks session/project
and capabilities, and rejects open_lane explicitly. Host/Origin/token and body
bounds precede dispatch; no URL-fetch, redirect-following or arbitrary-eval
capability is offered. Token lifetime/history redaction and authorization at
apply are retained. This does not prove a future generated-script import sandbox.

Scope boundaries retained in this verdict:

- Godot/Node evidence proves serializer conformance. Godot's artifact correctly
  says raw_wire_parser_conformance=false; Python owns raw HTTP/JSON admission.
- The mutable object is an in-memory mock counter. This is not Godot/Blender
  editor transaction, file publish, durable engine-state recovery, multi-host
  storage, Linux safe-open or GT-03/04 consumer evidence.
- Three symlink-related cases were skipped because symbolic-link creation was
  unavailable; the fourth skip is the non-Windows capability case. Windows
  junction/hardlink negative tests ran, but do not convert those skips into
  positive Linux or symlink-creation coverage.
- The package records Windows/AMD64, Python and consumer/tool hashes. It is not
  a full portability inventory or Linux/Android/device acceptance package.
- The journal's documented trusted private host namespace assumption remains
  necessary. This review does not grant agent-selected paths access to it or
  claim protection from arbitrary writes by the same OS principal.

Independent evidence and reproduction, from the 8-9-hh3d-3 product directory:

    python -B zdoc/reviews/20260915-gt02-s30-critic-a/verify_independent.py
    python -B zdoc/reviews/20260915-gt02-s30-critic-a/run_focused.py

Adjacent artifacts: verification.json, focused-host.json,
focused-stdout.txt and focused-stderr.txt. The verifier performs no engine
execution; the selected replay uses only private disposable journal/socket
fixtures. These scripts are critic evidence and are not source implementation.

Final decision for the exact closure above: VERDICT=FAIL; TICK=no.
