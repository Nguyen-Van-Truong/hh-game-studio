# S93 continuation of the bounded identity diagnostic

AUTHORITY=0. Supplemental only; never a GT-06 acceptance sample.

S91 completed 17 partial batches before the host status sampler omitted real
discovery/lease responses and accumulated a gap above 2000 ms. Its failed raw
data, native exit 2 during cleanup, observer exits and hashes are preserved in
the sibling S92-status-recovery packet. No partial prefix is resumed or promoted.

S93 uses the repaired command schema/validator, source closure
e05d69137d9b6ff303bf75abe47cf981ad336c41ab7945392cd6f8ae6df0e601 (51 files).
The profile remains 0cd5b53055853b592d4376a6b8a89d69c70ea5facc40dfc25ebd6fcd6dbf4d85.
The targeted TreeItem/Node3D probe and native patch bytes are identical to S91;
the existing controlled two-arm native smoke therefore remains applicable.
`reuse.json` documents helper equality. The inherited filenames/schema names
retain S91 to avoid changing the proven instrumentation; only run IDs, launch
directory and expected source binding are changed where necessary.

Run ID: gt06-s93-sparse-attribution-01. Preflight ID ends preflight-01.
Demand-only task: HHStudio.GT06.gt06-s93-sparse-attribution-01.
Receipts: launch-01/observer. No task dispatch before successful preflight.
Use register_task.ps1 -Command status for live state; do not infer liveness from
dispatch. Source/helper/profile remain frozen during a run. No other engine,
test or heavy audit alongside measurement. Watchdog limits are unchanged.

At terminal, read actual retained-handle exits and all Job/handle cleanup,
preserve sparse snapshots and failures, verify hashes, and diagnose before retry.
This new run is necessary because the sampler source changed; it is not a
rerun merely to repair metadata. The old S86 ObjectDB +2 remains unexplained.

Preflight-01 imported the unchanged native overlay successfully: actual Godot
55232 exit0, helper exit0, Job zero/closed. Its selected exact receipts and
source/helper maps are under preflight-evidence/. The repaired source bytes
are committed at b113587b. Full 1000-command HTTP schema validation also passed
on copied history before this import; no previous benchmark sample is reused.
