# S80 dependency bridge

AUTHORITY=0. Dependency comparison only; no native/test run, source edit, raw-tree
audit, critic verdict or acceptance. Exact map anchors, observed hashes, changed
paths and comparison time are in `s80-dependency-bridge.json`. These observations
are not a frozen final S80 source claim.

**Reuse the seven S79 service/reviewer runs and S69 managed replay within their
original scopes. Remint the affected benchmark evidence for S80.**

| Evidence domain | Declared dependency comparison | Disposition |
| --- | --- | --- |
| Seven S79 HTTP/adversary/reviewer runs | All seven 174-file maps equal the frozen backend map and current bytes. Current collector path membership is still exactly 174. | Reuse original scoped evidence; zero S80 benchmark-path overlap. |
| S79 reviewer Complete/Stop UI | Both five-file UI maps match. Service/adversary/reviewer drivers, bootstrap owner and remint launcher also match their frozen bytes. | Integrated reviewer/service behavior remains supported by these S79 runs. |
| S69 managed replay | All 159 runtime files and four repair/replay verifier sources match. | Preserve original native replay and S65 fault/repair02 provenance; no S80-caused remint. |
| S65 native04/native05 | Each historical 154-file map differs in `fixtures/play-observe/project.godot` and `godot-addon/observe/trace_bridge.gd`. These are outside S80's changes. | Retain historical functional/perf evidence; reject an exact-current runtime claim. S69/S79 supply current functional dependency matches. |
| S65 perf postprocessing | Both three-file collector maps match; native05's four verifier sources also match. | Reuse that postprocessing scope without relabeling the older native runtime. |
| S68 reviewer Complete02/Stop02 | Each 173-file map differs in `service.py` and `verified_journal.py`; current backend additionally contains `disk_journal_index.py`. | Historical integrated evidence only; use S79 for current service integration. |

The shared S79 backend closure remains
`644b90abfe769bf1a3654ba506cef50f8a38393f05f0be5ca7aae8050aaaa527`.
The comparison hashed only declared current source projections and seven S80
files: 190 distinct source/helper files, approximately 2.23 MB. No screenshots,
GLBs, journals, native logs, executable binaries or source-copy trees were hashed.
Prior execution/cleanup findings are retained, not independently re-proven here.

The **453-file S79 freeze is a broad historical snapshot**, including unrelated
benchmark drivers and unit tests. It is not the required runtime map of each
service lane. Five existing files in that snapshot now differ: native benchmark,
native diagnostic runner, campaign runner, assembly verifier and assembly test.
The readiness verifier and its test are new. None belongs to the 174-file backend
map, the five-file UI maps or the 159-file managed replay map. Current fixed
collector membership was checked from the accepted 143-file manifest plus the
fixture/observer/native-runner paths and backend additions; no collector was
executed. Benchmark profile and toolchain hashes remain equal to S79.

Do not rerun the historical `service-remint-01/verify.py` against changed live
benchmark files and describe that as an S79 failure: it deliberately requires all
453 live hashes to equal the old snapshot. Preserve its original freeze and raw
results. Conversely, do not replace the historical source hash with the current
benchmark hash or claim these were S80 executions.

There is a provenance label mismatch: `service-remint-01/REPORT.md` names HEAD
`24efe967`, while its immutable `freeze.json` records
`e4bbc1fe3a99d5860fb0c5ca9e163860b05f4d2a`. The freeze's exact byte hash remains
`adea95e23a6487f879e5e41a366530286869c0ccca05bcbd49fad68af5b70f28`.
This bridge retains the recorded head and relies on exact dependency hashes;
it does not retroactively resolve or rewrite that label.

S80 native aggregate index 1.3.0 and startup-readiness behavior require fresh
diagnostic/campaign evidence and affected unit checks on the coordinator's final
source. Earlier 1.2.0 aggregates cannot prove that behavior. The required full
campaign and two independent final critics remain separate, outstanding gates.
Any later change in a reused lane's dependency map invalidates this projection
until rechecked.
