# S97 coupled diagnostic - fixed candidate binding

AUTHORITY=0. Fixed run `gt06-s97-coupled-phases-01`; import preflight
`gt06-s97-coupled-phases-preflight-01`. Adapter preparation itself launched no helper, test, task or engine. The
current source freeze is recorded in `source-current.json`; execution requires
`freeze_status: ROOT_FROZEN`, the exact 51-file map and its source closure digest. Root completed the 24
targeted reader cases before setting this freeze; the tools plan and launch
receipts hold current execution status.
Missing, pending, malformed, unchanged S96, or expanded source maps fail closed.
The only permitted runtime delta from the retained S96 map is
`tests/replay/benchmark_native.gd`; actual current bytes are then checked by the
retained campaign collector. Profile remains
`0cd5b53055853b592d4376a6b8a89d69c70ea5facc40dfc25ebd6fcd6dbf4d85`.

The entry imports the hash-pinned S96 composition and changes only its run,
preflight, output, source-map, helper-map and request bindings. It delegates
execution to the original S93 supervisor/observer, S95 primitive collector and
HTTP phase recorder. Both S96 source files and all its original helper inputs
remain pinned at their real paths; the new entry/launcher/tests/map/README join
that map. Original helper bytes and new adapter bytes are copied separately.
The older source map is retained provenance and the reference for the allowed
one-file delta; it is never presented as S97's current source.

The PowerShell adapter checks the exact retained launcher hash before making
seven counted textual substitutions: three path references, root depth, run ID,
preflight ID, source digest, request schema and displayed task description.
The task implementation is retained: current user, hidden, demand-only,
register/start separate, one dispatch claim before COM, no triggers/retries,
same seven-slot outer Job and 7530-second watchdog. Inner wall remains 7410
seconds. All 35 full-mode batches, 1000 HTTP commands and 100 native cycles per
batch, original deadlines, status/counter gates and Stop semantics remain.

The native census deliberately reuses the **S96 ACK census schema and phase**,
including `_s96_before_ack`, `hh-studio.gt06.s96-ack-sparse-attribution` and
`hh-studio.gt06.s96-ack-sparse-post`. This describes instrumentation schema,
not old-run evidence. Every row must bind to the new run, source, PID, ACK
digest and original ACK receipt. Baseline ACK 4 and first later growth remain
the only census triggers. The unchanged overlay reverses byte-for-byte to the
S97 candidate, preserving its shared-open helper, `_write_batch`, original
ACK observed time, deadline check and heartbeat. No barrier extension exists.

HTTP keeps the 512-event ring, 64 active spans, first LOOKUP transport-failure
freeze, totals for other fixed routes and terminal-only snapshot after child
cleanup. Missing terminal output remains a gap. Nested spans overlap and
instrumentation cost is retained. The inherited `s96-composition.json` filename
also remains; its run ID/entry/source binding identifies S97. Its phrase
`source_files_are_unmodified_formal_base` means the frozen base is unchanged by
the disposable overlay, not that S97 has been accepted. The new request labels
that base explicitly `root_frozen_S97_candidate_not_accepted`.

Root's serialized validation/dispatch sequence:

1. Validate candidate regressions and freeze the complete source map. Run the
   four focused adapter checks in `test_coupled_phases.py`; also retain the six
   original S96 overlay/sanitization checks against the current candidate.
2. `python -B <this-directory>/coupled_phases.py --describe` is read-only and
   returns exact current request/helper pins. It refuses pending source.
3. `register_task.ps1 -Command register -HelperSha256 <request.helper_sha256>
   -PythonExe <pinned-python.exe>` registers only. Then run `coupled_phases.py
   --preflight` through the owned lane; its actual import exit/cleanup must pass.
4. `register_task.ps1 -Command start` checks the exact preflight/pins/XML, unused
   output/dispatch claim and all visible/hidden GT06 tasks before dispatch.
   `status` reports scheduler state only; `delete` requires no instances and a
   terminal task. Any source/helper change after a used identity needs fresh IDs.

Stop remains the original bound `HH-GT06-CAMPAIGN-STOP-1` record under the new
raw run. Require distinct actual supervisor/host/native/helper exits, closed
Jobs at zero active processes, released handles, original child cleanup and
external observer/scheduler terminal proof. Self-written returns do not prove
self-exit. Freeze/seal exact effective native text as well as the original
source, helper and raw receipts. Missing proof and failed prefixes remain
preserved. No formal PASS, acceptance dataset, or S96 root-cause claim follows.
