# S107 result audit and next boundary

AUTHORITY=0. Coordinator integration record, not a final critic signature.

The result worker independently verified packet manifest
`0cbad6e2ec833680910189bc8227bafa53bee7460173f9e0d82f7ffbecc6ed53`:
66 originals/copies, 72 manifest hashes, 48 capture references, current53 source
files, frozen10 helpers, campaign/context binding, native-index11 source entries,
batch hash and all40 row semantics/timestamps. Its recomputed arm/group metrics
matched the retained analysis. Maximum native heartbeat gap was568.226ms;
no dropped commands/telemetry. All actual target/helper/outer exits were0,
with natural tree exits and recorded Job/probe/wrapper release checks. Import
wrapper native CloseHandle remains UNKNOWN and live Stop was not exercised.

An initial Git-filter finding was withdrawn after checking actual bytes:
`.gitattributes` is `[42,32,45,116,101,120,116,10]`, a correct eight-byte
`* -text` line. The review had used absolute Windows-backslash paths with
Git stdin path filtering, which missed the attributes, and misread the escaped
repr. Repeating with repository-relative forward-slash paths gave74/74 exact
filtered/unfiltered bytes. Coordinator's separate staged-blob comparison gave
85/85 exact bytes, zero mismatches. No evidence or manifest was rewritten.
Lesson: use byte arrays and actual staged blobs, not escaped display strings,
to diagnose newline preservation.

The contract worker independently concluded that S107 supplies no causal repair
for S102. Stock native benchmark save, public staged ResourceSaver publication,
and the formal1000HTTP+100native workload are different paths. Removing preview
would change executed coverage. No additional ABBA or unchanged formal retry
follows from the measured78.54415ms mean contrast.

Next useful preparation is bounded owned-thread CPU attribution through actual
b6, preserving stock save/full mix/all gates. The native research worker checked
pinned Godot sources: `OS.get_thread_caller_id()` returns logical TLS IDs, not
Windows TIDs. Obtain MAIN_WINDOW's HWND, bind its creator TID to the retained
editor process, corroborate owned windows, retain a query-only thread handle,
and preserve creation time/handle-close evidence. Ambiguity is UNKNOWN.

CPU accounting over enclosing sample windows is not an exact call CPU measure
and cannot distinguish disk/GPU/locks/scheduler wait. Host/native clocks require
ACK transport bounds: timestamp BEFORE publish, native receipt time and host
receipt observation. Publish's return is after rename/readback and is not a
valid lower bound. Keep quantization, interval uncertainty and observer costs;
do not subtract them from any original gate.

Supplemental worker review briefly encountered a workspace-credit error; bounded
follow-up tasks subsequently ran. This is not an external machine blocker or a
reason to invent review completion. No final acceptance critic has been invoked.
