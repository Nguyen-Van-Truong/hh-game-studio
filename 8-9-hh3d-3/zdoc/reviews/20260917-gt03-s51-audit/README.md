# S51 diagnostic evidence audit

AUTHORITY=0. Acceptance, formal GT03 acceptance and public ACK remain false.
This verifies consistency of historical host/process/source records; it does
not reopen deleted native roots, issue registered receipts, or prove live
editor adoption after active selection. No engine or daemon is invoked.

Required final packages are `s51-editor-01`, `s51-components-01`, `s51-linux-01`,
`s51-profile-01`, `s51-semantic-04` and `s51-host-death-01` (all prefixed
`20260917-gt03-`), with exact closure
`55f15f2a431b8b065565143d456bd4cafbc59bca78a99492e3674ef93a97007c`.
The completed raw unit marker and stderr bind 471 tests, zero failures/errors/
skips; editor checks are 103/10/31, Linux fixtures 11, eligible profile cases 5,
and exact candidate/editor semantic comparisons 6. The earlier semantic-03
diagnostic with origin-source drift is never substituted for semantic-04.

The adapter reuses S49 verification logic pinned to SHA256
`ad040b4512fbdfd865131a7d281ca958fad853cee36b1c21e3e0b2870338271f`.
Only the two exact host-death marker literals are adapted in memory, then S51
paths/counts are explicitly bound. The earlier verifier file stays unchanged;
earlier test results and signatures are not reused. The delegated components
helper is pinned to `af144df7107fe1dbaba4f2f8242452962a9d514805255f70b4eb31bcea9ea55e`
and reuses the pinned S50 descriptor checks plus native evidence helpers.

The inherited checks verify current/frozen source inventories, raw actual
Windows target/wrapper exits and Job drain, Docker policy/identity/exit/wait/
removal, bounded stream facts, exact readonly inputs and harness, eleven
hostile expectations, five eligible profile observations and host-death/orphan
recovery. Every principal package has a mandatory complete artifact inventory.

Semantic verification reconstructs all six exact qualified bundles and fixed
Windows diagnostic overlays from frozen bytes. It re-evaluates each Linux
report and native lifecycle, binds the Windows engine-log marker to the actual
target PID and guarded editor snapshot, then compares full canonical JCS bytes
and independently recomputed hashes with no epsilon. It checks the archived
canonical byte files, baseline raw replay fixture, revision change on masked
script-default replacement and both summary copies. Final source/snapshot
unchanged flags must all be true. The diagnostic overlay is explicit; the
candidate helper never invents a live-editor identity.

Components verification checks semantic observation→final manifest rebinding
without changed engine inputs, native staging/descriptors, selector A/B and
readonly reopen facts. Native roots were closed/deleted by their actual owned
producer, so the auditor's conclusion remains historical evidence consistency,
not independent current native root verification or selected editor adoption.

Coordinator owns the artifact manifest, host-death probes and Git byte proof.
The manifest must enumerate every byte in each named package, including this
audit directory except its manifest/generated verification outputs. Adding an
artifact after manifest creation requires regenerating that manifest.

Run in fresh Python processes after all final packages and helper pins exist:

```powershell
python -B 8-9-hh3d-3/zdoc/reviews/20260917-gt03-s51-audit/test_evidence.py
python -B 8-9-hh3d-3/zdoc/reviews/20260917-gt03-s51-audit/test_components_check.py
python -B 8-9-hh3d-3/zdoc/reviews/20260917-gt03-s51-audit/verify_evidence.py
```

Optional `index` or `HEAD` arguments reuse bounded exact Git blob comparison;
they do not stage or commit. A failed audit writes no successful summary.
Tests use in-memory corruption or temporary dependency copies; final artifacts
and frozen runtime source remain unchanged.

Actual offline test completion: 14 adapter/parity tests passed in 4.321 seconds;
the delegated components helper passed 11 tests in 24.211 seconds, both exit 0.
These include recomputation of the actual final six-case semantic package and
actual components baseline, plus corruption cases. Whole-package verification
and Git proof require the coordinator's final artifact manifest.
