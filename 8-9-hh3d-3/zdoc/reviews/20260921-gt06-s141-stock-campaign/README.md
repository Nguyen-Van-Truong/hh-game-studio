# S141 stock campaign

Authority 0. This is execution preparation, not GT06 acceptance.

S140 finished the planned seven-batch phase diagnostic and found no material
batch 4→5 native/ACK/assembly retention discontinuity. S131 remains unresolved;
no runtime patch is justified by that prefix. The next evidence needed is the
uninstrumented full campaign. See S140 `coordinator-terminal.md` for the decision,
failed attempts and limits. Do not run another equivalent diagnostic first.

`launch_stock.py` calls unchanged `run_campaign` directly, with no monkeypatch,
instrumented overlay, alternative allocator, timing/threshold/baseline change,
priority adjustment or extra child in the original Jobs. Its default is inert
pin/workstation verification. `--run` has an exclusive one-use claim and refuses
an existing campaign ID. Original campaign stops on its first exception. Do not
automatically retry the failed pair or erase Stop.

Freeze: 231 benchmark/installed union files, 3 GT05 input artifacts, launcher and
S139 provenance = 236 exact copies. Binaries and workstation are separately
pinned. Stock per-pair initial/editor snapshots and captured runtime closure bind
generated project contents; final closure must include those future artifacts.
`prelaunch-01` preserves an inert check failure: tuple/list JSON representation
differed for the same workstation. Fixed using the stock canonical serialization
comparison; no engine was started and no workstation acceptance field removed.

Detached task uses unchanged S129 passive observation. Observer request itself
has diagnostic authority only and never grants dataset eligibility; stock
campaign artifacts go through their unchanged strict assembler. Passive bounds
are 85,800 seconds and a 24-hour scheduler cap, within the prior campaign cap;
the per-pair 7,410-second owner bound is unchanged. Actual launcher exit/handle
receipt is recorded separately; scheduler result alone is not proof.

Campaign ID `gt06-s141-formal-01`; raw under `studio/.local/reviews/`.
Only one campaign runs at a time. Do not run engines/tests/heavy source audits
alongside its measurements. Check fresh process identities and short tails.
Healthy runs continue under the ten-minute task heartbeat. After terminal,
verify raw outcomes, actual host/editor/helper exits, Jobs/handles/Stop/source
pins, all attempts and full 10×35 dataset before final evidence freeze and two
new independent critics. There are currently zero accepted full runs.
