# Coordinator pre-dispatch validation

AUTHORITY=0. Implementation/preflight checks are not independent acceptance
critics and do not satisfy GT06's final benchmark or review gate.

The unchanged formal51 source closure remains
`564e5b7877f49d33a1af84eaf3b3a7ddf4ea2f846c4161eb694c5c40514fc752`.
The nine-helper S96 closure frozen in the registered request is
`49305105575402570035bbff7f3f7abdcc5a98653b8cc994932b4ae07f921795`.

- Recorder unit02:12/12, actual4300/helper20936 exit0, owned tree verified.
- Real HTTP smoke02: actual47412/helper17632 exit0, tree verified. Ten unchanged
  diagnostic commands/two effects, then two explicitly injected disconnects.
  The commands failure did not consume the later lookup snapshot. Lookup and
  retry joined distinct client/server root IDs through their socket port pair
  and QPC lifecycle. All17 phases appeared, no missing identity, dropped span,
  event eviction or terminal unfinished span. This is not S93 reproduction.
- Composition unit01:6/6, actual53876/helper53992 exit0, tree verified, five
  test/input files unchanged. Exact native reversal preserves `_write_batch`,
  original ACK timestamp/deadline/heartbeat, with the hook after ACK validation.
- Actual copied-project import preflight: Godot42632 exit0, import helper0,
  Job zero/closed and no retained handle. Parent24936/helper9388 exit0, tree
  verified.75 exact preflight copies are retained in `preflight-evidence/`.
  Import validates compilation/inactive startup; it does not execute ACK.

The disposable native driver SHA256 is
`de1838cf836e7dfd9a80fa05c7636774e43da0121dc3d7b551e02b90f5f746b1`.
Its first real ACK and batch4 sparse baseline still need observation in the
single original-dimension coupled diagnostic. No separate reduced campaign is
represented as proving that path.

Before freeze, review caught that refreshing the prepublication end time could
break its barrier-issued-time binding. The final patch instead samples at the
actual failing post-ACK lifecycle and leaves that entire publication unchanged.
It also filters the one failure snapshot to lookup. An earlier claim that the
current benchmark deliberately drops submit responses was incorrect: that was
historical test context. Current producer code has no demonstrated injection;
the commands-before-lookup regression and HTTP cuts here are explicit tests.
Original unit01/smoke01 recorder evidence remains tied to its older helper.

History copies and disposable commands/journal/index directories from the HTTP
smokes remain local and excluded from Git. Their hashes/provenance are retained
in the freeze/report records. Source, helper, raw capture and scheduler domains
remain distinct. No threshold, timeout, retry, fsync or lock policy was relaxed.
