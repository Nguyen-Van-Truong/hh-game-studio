# S118 import boundary (AUTHORITY=0)
S117-02 ended STAGE_WALL_LIMIT before any batch. Original142 observer samples
showed target CPU progression, no terminal import milestones beyond .gdignore,
no stderr and target natural exit unknown. Do not infer deadlock or RAM cause.

This single fresh import-only diagnostic adds --verbose to the exact pinned
engine and original campaign project recipe. All native stage20s/15CPU/2GiB
limits remain enforced. It does not start the editor workload or HTTP producer.
The purpose is to locate the import phase using retained engine logs. Logging
adds overhead and the result is excluded from F13/F14. No formal retry follows
a non-reproduction automatically. Read raw invocation/capture/observer/actual
exit, source pins and supervisor receipts. Preserve failures and UNKNOWNs.

Run --check once, then launch.ps1 once. Never reuse output IDs. The helper is
outside imported runtime; execution manifest binds its bytes and initial scene.
