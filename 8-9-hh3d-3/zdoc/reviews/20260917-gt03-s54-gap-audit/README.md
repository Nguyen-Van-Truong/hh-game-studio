# GT-03 S54 readiness audit

Read-only implementation audit on 2026-09-17, HEAD `babfadd` plus the coordinator's changing S54 tree. **Not an independent acceptance review; no TICK or GT-03 acceptance.** Scope is the current tools plan §GT-03 (lines 797–809), §2.2/2.3/2.5/2.6, TQ00/TQ03, and TX02/03/06/09/13/15. No tests or engines were run for this audit. Root's capacity/Stop repairs and next authenticated edit probe were already active; their completion is not assumed below.

Evidence keys (each retains its own frozen closure):

- **S53**: `20260917-gt03-s53-audit`, scene publication01 (17 checks), script-publication02 (31), Stop01 (19), and units01 (557); closure `31a66517322bc57665aa316c53a88686ee27037a38ff055b1744327ad2464cec`.
- **E54**: editor03, closure `abee9b1cfd4803497aef549c87c59f08021d73591213c2afcf003467b6ba9a66`: 29 Python, 40 registered edits, 39 exact rotation/history, 19 generation checks; actual target/wrapper exits 0 and clean owned Jobs. editor02 and HTTP edit-publication01/02 remain FAIL.
- **R54**: recovery-publication01 plus recovery-publication-audit, closure `469b22d6788ef63c7ca4d6e7dc91e703494db439834dffacdef7d476a9464bdc`: actual Linux→HTTP save→capture→crash92→fresh HTTP/fence/editor→durable terminal→fresh-host retry; 14 checks, native nine-record custody binding, 20 audit corruption rejects. Native storage01's three cuts use synthetic original engine/authority facts.

| Requirement | Implemented now | Evidence | Actual remaining gap for freeze |
|---|---|---|---|
| Typed main-thread CRUD/resource edits, stable IDs; TQ03/TX02 | `scene_commands.gd`, registered `EditorOwner.prepare_edit/apply_edit`, bounded Node3D/MeshInstance3D/BoxMesh; V5 checkpoint before effect; exact readback before durable edit reply. | E54 actual create/update/remove, manual-edit rejection and generation guards. | Finish the already-running authenticated CRUD→Undo/Redo→save/reopen chain. HTTP edit01 exposed transform cache normalization and is superseded only by E54 component proof; HTTP edit02 reached create/update/Undo/Redo/remove but `edit.restore` was correctly rejected `GODOT_DEADLINE_EXPIRED`. Renew/reacquire the bounded lease in the harness; do not weaken deadlines. |
| UndoRedo/checkpoint, preserve manual edits; §2.6/TQ03/TX06 | Full checkpoint capture, V5 immutable checkpoint/observation blobs, exact revision/history guard. Transform components now normalize from the unchanged matrix before recording history. Edit COMMITTED explicitly means an observed editor-session effect, not saved files or persistent history. | E54 exact matrix/history and manual-edit preservation; owner tests cover stale precommit observation. | Integration proof above, plus crash after an actual admitted V5 edit (see cut table). No need to redesign the now-proven transform/history mechanism. |
| Staged scene+script publication; §2.3/TQ03 | Complete 11-file bundle, Linux parse/import/readback, protected same-volume selector CAS, retire-before-fresh-editor activation for script changes, exact durable response. Fixed declarative `.gd` profile, full-text ≤16 KiB; no arbitrary GDScript. | S53 scene17/script31, including dirty scene plus new script default/source, generation and original response replay. | Preserve existing scoped implementation; remint affected integrated paths on the final frozen source after S54 settles. General scripting, arbitrary resources and artist-project writes are outside the advertised profile, not new GT-03 blockers. |
| Public discovery and actual dispatch; §2.2/TX15 | Static catalog flags remain false by default. Live `GodotPublicationOwner.discover` enables exactly nine operations: inspect, preview, node create/update/remove, undo, redo, save, script replace. Control lookup/Stop are separate routes; RecoveryHost separately advertises `project.reconcile`. Stop leaves only inspect/preview enabled on a healthy publication owner. | S53 authenticated catalog/save/script; E54 component edits; R54 actual recovery discovery/auth. | Match final discovery to final tested backend/capacity state. RecoveryHost currently computes `runtime_enabled` only from its fresh session/held flag, while the durable journal may prohibit reconciliation after STOP; expose that held/stopped reason instead of suggesting executable recovery. |
| Single writer, expiry, fencing **and FIFO**; TX06 | Registered exact grants/leases, one active writer/effect, expiry/fence rechecked at every effect; immediate `GODOT_LEASE_BUSY`/`GODOT_OWNER_BUSY`. | Session tests and S53/R54 scoped authority proof. | **Implementation gap:** no arrival-ordered writer waiter queue in `PublicationSession.lease`/owner routing. TX06 is explicitly part of GT-03 DoD; GT-07 later expands scheduling, but does not defer this single-project FIFO requirement. Implement a bounded wrapper around all native lease calls; no bypass/renewal ahead of waiters, cancellation/expiry/Stop, exact ticket outcomes. |
| Limits before admission; §2.2/TQ00 | Catalog caps node/depth/payload/script/deadline. Editor16 effects, validator4 attempts, session64 commands, V4/V5 256 events/512 KiB snapshot, V5 63 blobs/8 MiB, recovery32 suffix events. New owner code reserves two editor effects for a later save and validation capacity before accepting an edit; V5 reserves checkpoint/terminal storage. | Existing cap/reject tests; new owner capacity tests are present. | Root is repairing this now. Verify admission does not strand an accepted edit without its final save/terminal/Stop budget. Advertise effective owner-lifetime limits and exhaustion behavior in discovery/docs, rather than only the broader scene/payload limits. Do not expand budgets merely to hide probe expiry. |
| Same-app crash recovery; §2.5/TQ03/TX09 | Read-only selected files; fresh registered higher fence and owned editor; same protected log `ADMITTED→READBACK→TERMINAL`; original/recovered bytes distinct; no writable restart. V4/V5 prefixes and actual native checkpoint blobs supported. | R54 proves one complete authenticated pre-captured-event crash path. Storage01 proves selector/CAS and orphan witness mechanics only. | Actual engine/authenticated coverage for remaining meaningful cut classes below. Do not count synthetic receipt storage tests as engine recovery or extend the one cut to all phases. |
| Stop/revoke/manual intervention; §2.6/TX06 | Per-phase admission prevents later effects; started effect reports draining/UNKNOWN. PublicationOwner now owns a durable idle-Stop drain, reporting PENDING/UNKNOWN/DURABLE. Durable original STOP is rejected by recovery; explicit original UNKNOWN restoration is allowed only with no STOP and selected exactly last-good. | S53 actual Stop during live Linux validation, preserved dirty editor/selection and durable UNKNOWN; pure authority/owner/mixed-prefix cases. | Native evidence for the new idle Stop and Stop/revoke at edit/retire/adopt/commit boundaries is incomplete. RecoveryHost Stop remains an in-memory session latch; its restart behavior must explicitly preserve the Stop policy before claiming durable Stop/reconnect semantics. It must not silently classify deliberate Stop as a recoverable crash. |
| Half publish/readback failure/response loss; TX09 | Atomic protected selector, journal phase/custody guards, failure preserves last-good; exact terminal lookup occurs before retry admission. | S53 native journal/CAS tests; R54 raw protected stream plus HTTP retry/reopen; owner tests include lost return after durable commit. | Fill actual post-CAS/pre-readback and terminal-before-return cut classes below. Reuse accepted GT-02 storage race/alias primitives and existing S53 lower-level proofs; do not rebuild their test matrix unnecessarily. |
| Path/schema/auth/role confinement; TX03/13/15 | GT-02 canonical/parser/transport/native safe-open reused; fixed roots/profile, no eval; main-thread editor and isolated Linux validation; no Play mutation capability. | Accepted GT-02 plus S53 profile/auth/path tests and isolated parse/readback; R54 source/runtime identity audit. | No new arbitrary-path, Play/input/capture, GPU or cross-app implementation is needed for GT-03. Those consumers remain GT-06/07. Final changed transport/grant seams need focused conformance, not replay of every historical baseline spike. |
| Freeze/evidence/review; TQ00/GT-03 DoD | Separate packages carry actual exits, runtime hashes, manifests and scoped audit records. | S53 checkpoint; E54/R54 components retain distinct source closures. | One final frozen dependency closure and requirement→artifact map, clean owned processes, then **two independent critics on that closure**. Current component reviewers supply no acceptance signature. |

## Recovery cuts still needed

| Cut class | Current coverage | Smallest remaining proof |
|---|---|---|
| Capture reached engine, before durable CAPTURED | R54 real authenticated host exit92; fresh editor restores unchanged initial last-good; exact retry/reopen. | Already proved for its closure. Include in final integration inventory; no new recovery design. |
| V5 EDIT_READY / effect applied but before durable EDIT_COMMITTED | Typed mixed-prefix tests and actual edit checkpoint component; no full crash chain. | One actual authenticated edit crash, preserving original command outcome and restoring last-good with explicit history loss. |
| Staged/validated/retired before selector CAS | Ordinary journal/authority tests and S53 retirement path; no full fresh-authority recovery crash chain. | Cover the script-specific retired-editor boundary with scene+script binding and a fresh editor, preserving selected last-good. Adjacent phases with identical state/authority can share a documented fault-class fixture; do not claim unexecuted cuts individually. |
| Actual selector CAS before SELECTED/readback | Storage-only actual CAS case; original engine facts synthetic. | Real candidate scene+script selected, then crash; fresh recovery observes that exact candidate or reports the existing safe hold. No selector rollback or writable restart. |
| READBACK/COMMITTED before reply; log durable ahead of custody | Native exit91 orphan mechanics and original response-loss unit tests; full terminal HTTP retry/reopen after ordinary return. | Actual publication/readback/terminal crash plus exact reply recovery; unwitnessed COMMITTED remains blocked as an original success until fresh readback and a distinct durable recovered result. |
| Deliberate STOP/revocation versus crash | S53 active-validator Stop; pure durable-STOP rejection and explicit UNKNOWN-last-good tests. | Close the new idle-Stop persistence proof and recovery-host Stop restart policy; actual mid-effect Stop must stay held/drained, never automatically resume. |

Smallest next concrete implementation after root's active capacity/Stop repairs: **bounded per-project FIFO lease admission**. A new queue module can own arrival/ticket state, recheck the exact registered grant immediately before calling the existing `PublicationSession.lease`, and retain terminal ticket results after reply loss. Keep queue/grants volatile across restart; durable mutation/recovery remains in the existing journal. Root should route every writer lease request through that wrapper before claiming FIFO. This is smaller than introducing GT-07's dependency scheduler or generic writable recovery.
