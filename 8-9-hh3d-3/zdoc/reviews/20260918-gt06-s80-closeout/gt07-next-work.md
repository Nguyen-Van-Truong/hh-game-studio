# GT-07 next work — preparation only

2026-09-18, approximately 14:26 Asia/Saigon. AUTHORITY=0. Current tools plan S80 still has `CURRENT_VALID_WP=GT-06`; GT-07 remains PLANNED and requires GT-05 plus accepted GT-06. This note prepares later work while the GT-06 benchmark runs. No gate is opened, no implementation/plan/runtime change or acceptance verdict is made. Only this new report was written. No engine/test/process operation, installation or raw/hash audit was performed.

## Exact target

The plan's GT-07 block allows `studio/host/leases/jobs/activation` and adapter transaction tests. Build FIFO/expiry/fencing/dependencies/bounded concurrency, the §2.5 active manifest and safe manual-edit reconciliation. Required verification: competing writers on the same `.blend`/`.tscn`, expired late result, correct owned-job termination, cuts at PREPARED/VALIDATED/ACTIVATING/COMMITTED/reload/ACK, file lock/disk full/cross-volume/journal full, preserved source and no partial active release. DoD remains TQ07/TX05/06/09/11/13/14, TX18 fairness under the explicit contract, Stop priority, no starvation or overwrite of newer manual edits, clear secret-free states, and two independent critics.

The smallest missing product behavior is **one recoverable activation of the existing fixed Blender→GLB→Godot fixture**, with a separate source-save result and asset-active result. Do not rebuild the existing safety substrate or promise a distributed transaction across Blender and Godot.

## Reuse versus demonstrated gaps

| Existing implementation / evidence | Reuse exactly | Remaining GT-07 gap |
|---|---|---|
| `host/core/journal.py` and `JOURNAL.md`: durable pending/terminal reservation, dedupe/tombstones, project+target lease epoch, `lease_guard`, revision checks. `host/replay/verified_journal.py` and derived indexes supply the current optimized access path. | Existing journal semantics and native protected storage/custody/barriers; no new generic database or second independent success authority. Use the eventually accepted GT-06 closure for changed index dependencies. | Journal lease acquisition alone is not fair cross-project/session job scheduling or a dependency graph. A checked lease followed by an unguarded later publish is insufficient. |
| `godot-addon/publication_fifo.py`: bounded nonblocking FIFO for one exact PublicationSession; volatile tickets deliberately do not restore. `host/blender/writer_journal.py` + `durable_session.py`: durable FIFO grant intent, native arming/readback and late-fence checks for one fixed Blender GUI. | Adapter-local admission and identity checks; preserve existing Stop/UNKNOWN behavior. | No shared project/session fairness or resource/dependency arbitration is established by these per-owner queues. No durable cross-app transaction continuation follows from restoring a queue ticket. |
| GT-03 `GodotPublicationOwner`, publication journal V4/V5 and `PublicationRecovery`: owned editor, selected protected bytes, staged validation, CAS, readback, exact receipts, fresh-authority recovery and durable Stop. | Existing scene/script transaction components and their accepted fault classes. | Their exact eleven-file bundle (`bundle_v2.py::FILE_PROFILE`) has no GT-05 asset payload. It cannot be relabeled as a general GLB activation API. Recovery does not grant arbitrary writable restart. |
| GT-04 `BlenderPublicationOwner`: fixed immutable checkpoint/GLB publication; `recovery_owner.py`: fresh read-only GUI checkpoint inspection. `client_writer_owner.py` explicitly keeps `public_ack=false`. | Native Blender GUI ownership, fixed supported edits/export, protected checkpoint, readback and owned cleanup. | Read-only reopen and receipt history do not reconcile an interrupted cross-app command or make the Godot asset active. Existing checkpoint recovery requires the exact cleanly closed predecessor; it is not crashed-host reconstruction. |
| GT-05 `pipeline/snapshot_owner.py`, `snapshot_state.py`, `snapshot_provider.py`: four verified payloads, custody/event journal, selector and idempotent staged publication. `pipeline/godot/consumer.py` and `run_consumer.py`: fixed isolated import/reimport/native observation. | Existing original fixture, payload/manifest admission, semantic/import checks and tested consumer inputs. | Snapshot selector is explicitly `snapshot_kind=staged-assets`, `public_ack=false`, `editor_activation=false`. There is no cross-app active-release owner. Consumer preparation accepts an isolated coordinator directory and grants no publish authority. |
| GT-06 ReplayService/ReplayTransport and owned process runners: bounded work, reserved control/Stop, immutable Play snapshots, PID/start/Job observations. | Control-lane separation, process identity/cleanup and runtime provenance after GT-06 acceptance. | ReplayTransport explicitly omits FIFO lease routes; its per-owner lock is not a multi-project scheduler. New activation must preserve these controls while resource admission is saturated. |

## Ordered work, at most three disjoint file leases

**Start only after GT-06 acceptance.** Before coding, agree one small fixed-fixture handoff between the scheduler and activation owner: registered owner identities, command/digest, complete source/asset/active-revision bindings, finite resource/deadline limits, dependency IDs, Stop latch and phase/result. The activation owner owns its definition; scheduler/native fixtures consume it. No operation-catalog expansion, arbitrary path intake or generic execution lane.

| Order / file lease | First concrete files to create or edit | Small deliverable and verification |
|---|---|---|
| **1. Admission and resource scheduling** | Proposed new `studio/host/leases/registry.py`, `studio/host/jobs/scheduler.py`; its exclusive tests `studio/tests/transactions/test_gt07_scheduler.py`. Read existing core journal and adapter admission owners without changing them by default. | Add bounded FIFO per contended target, fairness across admitted project/session queues, dependency readiness and bounded engine/resource slots. Acquire a complete deterministic resource set or leave work queued; do not hold one app lease indefinitely while waiting for another. Recheck registered fencing/expiry/revisions at effect boundaries. Persist uncertainty/Stop through the chosen transaction owner rather than replaying volatile grants. Verify competing targets, expired late results, dependency failure, queue/result caps, cancellation during grant, fairness under flood and Stop access. |
| **2. Fixed cross-app activation and recovery** | Proposed new `studio/host/activation/contract.py`, `state.py`, `owner.py`, `recovery.py`; exclusive `studio/tests/transactions/test_gt07_activation.py`. Start with contract/state, then wire the registered existing producers and a fixed consumer bridge in this host namespace. | One typed transaction history coordinates publish/cancel and owns the public terminal outcome. PREPARED binds both source and current active revisions; VALIDATED binds the verified immutable GT-05 release; ACTIVATING persists intent before selector CAS; COMMITTED requires complete selected release plus actual consumer reload/readback and durable receipt. On restart, inspect journal/selector/hash, then fresh registered authority completes or restores last-good only while the expected revision still matches. A newer manual edit preserves both versions and returns a reconcile conflict. Component `COMMITTED` from a staged publisher cannot become a cross-app ACK. |
| **3. Native transaction fixtures and evidence** | Proposed new `studio/tests/transactions/run_gt07_activation.py`, `run_gt07_fault_matrix.py`, `test_gt07_evidence.py`, plus owned fixture helpers under `studio/tests/godot/gt07_*` and `studio/tests/blender/gt07_*`. No production adapter/catalog edits in this lease. | Reuse existing original assets, validators and engine owners. Author bounded fault fixtures against the agreed interface while leases 1/2 build. After integration, run one serialized native matrix demonstrating active selection, fresh editor readback, restart reconciliation and source preservation. Keep synthetic state/clock tests visibly distinct from actual app/process/storage evidence. |

Leases 1/2 can proceed independently once the handoff is fixed; lease 3 can author fixtures then. Native integration follows both and is serialized. New files above are proposals, not files created by this preparation. If an existing owner lacks the fixed release-loading/readback hook, implement the narrow registered bridge under `host/activation` and verify it through adapter transaction fixtures. Do not widen `bundle_v2.FILE_PROFILE`, arbitrary `.blend` intake, public command catalogs or accepted engine APIs as an incidental shortcut. Any unavoidable change outside GT-07's file scope must first be recorded as a concrete scope gap.

## Minimum required native matrix

1. **Positive activation and real conflict:** two registered writers contend for the same supported `.blend` and `.tscn`; oldest eligible work proceeds, stale writer/expired result cannot publish. Activate baseline then the existing edited fixture; selected immutable release, actual imported geometry and live editor source/revision/readback agree. Preserve a later manual edit and the worker candidate; an explicit new reconcile decision/revision is required before applying either.
2. **Phase cuts and fresh recovery:** terminate the owned transaction worker at each required PREPARED, VALIDATED, ACTIVATING, COMMITTED, reload and ACK boundary, including selector-switched/readback-missing and durable-terminal/response-lost distinctions. Restart with fresh process/session authority; inspect witnessed history and active bytes, obtain actual editor readback, and show exact retry without duplicate object/output. A stopped command remains stopped after reconnect. Do not reconstruct authority from an old PID/ObjectID or caller-written receipt.
3. **Storage failures:** real locked destination, controlled bounded disk/quota exhaustion and cross-volume attempted publication; journal-full/reserved-terminal, partial-write/fsync/readback faults. Keep original source/last-good selected and report UNKNOWN/RECOVERY_REQUIRED whenever durable completion is unproven. Accepted primitives may be reused, but injected `OSError` alone is not a new actual disk-full/antivirus/power-loss claim. Missing environment proof is a specific GAP, not implicit PASS; no installation is authorized by this note.
4. **Fairness/Stop/ownership:** multiple bounded project/session queues with blocked and independent dependencies; demonstrate eligible progress rather than FIFO within only one owner. Saturate admission while lookup and Stop remain available. Cancel during activation/grant, rotate/revoke a session, lose a response, replay an old job identity and present a mismatched PID/start tuple. Only the owned process tree can be terminated; capture real exits, drain/closed Job and handle release. Verify concurrent Play keeps its immutable source/PID/camera binding while editor activation changes the next release.

Finish with focused state/admission tests, the new native matrix, affected adapter regressions, requirement→evidence bindings and two new independent critics on the same frozen closure. Reuse unaffected accepted lanes by dependency; do not rerun all historical suites for a report/helper-only change.

## Precise evidence reuse and effort to avoid

- **GT-03 S55:** `zdoc/reviews/20260917-gt03-s55-audit/acceptance.json` and `s55-gate-map.md`. Reuse local `fifo-01` two-writer/expiry/cancel/Stop proof and the selected save/script/edit/recovery/selector-CAS/witnessed-terminal cuts for unchanged components. These do not prove the new cross-app transaction cuts or broader fairness. Its gate map already records equivalence for adjacent unexecuted hooks; do not turn every synonymous hook into a fresh mandatory matrix row.
- **GT-04 S60:** `zdoc/reviews/20260917-gt04-s60-audit/README.md`, `coverage.json`, source/execution closures and the two S60 final critic records. Reuse fixed native edit/export/FIFO/checkpoint/cleanup and protected publication results within their accepted scope. Do not re-run all three fixed publication profiles merely because a scheduler wrapper was added; select regression by changed dependencies. Do not upgrade its `public_ack=false`/read-only recovery limits by citation.
- **GT-05 S64:** `zdoc/reviews/20260917-gt05-s64-acceptance/acceptance.json` and `20260917-gt05-s63-audit/requirements-s64.md`. Reuse original/edited asset chain, actual import/reimport, four payload admission, same-ID retry and eleven native storage cases including STAGED supplement. Its report explicitly leaves active replace/scheduling to GT-07 and distinguishes injected I/O from actual storage faults. Rebuilding rigs, rendering all 68 PNGs, extending the catalog or repeating unchanged semantic rejection suites is not the first GT-07 task.
- **GT-06:** only the eventual accepted final manifest unlocks dependency reuse. Current S79 service-remint report is useful evidence to carry forward if its exact dependencies match: actual saturated work with priority lookup/Stop, revoked result UNKNOWN, stale capture rejection and owned cleanup. The running S80 campaign is not acceptance evidence yet. Do not rerun its complete 10×35 benchmark merely to validate a report; rerun affected measurement only if later source changes invalidate its dependency/performance claim.
- **§2.6/TX16:** preserve manifest/trust compatibility assumptions and clear queued/running/draining/unknown/reconcile states, but do not duplicate GT-08 producer signing/verifier or GT-10 installer/update/key-rotation implementation inside GT-07. The plan assigns those exact signing/package fixtures to later gates. No production signing credentials are a prerequisite for this preparation.

All reuse remains tied to original accepted source/evidence and current dependency equivalence. No old critic signature signs the proposed activation/scheduler code. No GT-07 verdict is supplied here.
