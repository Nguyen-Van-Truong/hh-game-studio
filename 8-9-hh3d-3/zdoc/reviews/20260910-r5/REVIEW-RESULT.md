# HH3D S14 — implementation resumed, acceptance pending

Owner has authorized implementation in dependency order and, on 2026-09-10,
requested official Grok CLI workers using `grok-4.6`, reasoning `xhigh`.
The CLI help does not expose a separate `fast` flag. No model substitution is allowed.

The two canonical TXT plans remain the only progress authority. GT-01 is
IN_PROGRESS; GT-02 and HH World implementation have not opened. Owner permission
does not substitute for runtime, independent critic, legal or human acceptance.

S14 corrects stale PLAN_ONLY and Codex-only worker policy, active review/routing
links, and the static validator's obsolete assumptions. Independent workers
review immutable plan inputs while two other workers implement disjoint GT-01
bootstrap/fixture areas and one researches official toolchain provenance.
Their results require coordinator inspection before integration; a toast or
CLI exit 0 is not an acceptance verdict.

## Evidence and limits

- `freeze-s14.json` binds the complete bytes of both current plans.
- `static-s14.json` and `selfcheck-s14.json` are generated by the commands below.
  They prove only the checks actually recorded there, never runtime correctness.
- Historical S6–S13 records do not certify S14. Missing tracked historical files
  were recovered from baseline `b4195ca` without overwriting edited files;
  `history-recovery.json` lists these recoveries. There was no historical cleanup.
- Existing GT-01 run-06 is a diagnostic candidate with an incomplete executable
  closure and no full fixture/headed/Blender proof. It is not a completed GT-01.
- No hundreds-of-millions capacity, public readiness or legal signoff is claimed.

## Reproduce current static checks

From the repository root:

```powershell
python 8-9-hh3d-3/zdoc/reviews/20260910-r5/validate_plans.py --freeze
python 8-9-hh3d-3/zdoc/reviews/20260910-r5/test_review_s14.py
```

`dispatch_batch.py` prepares a fresh isolated batch; `Launch-Batch.ps1` starts
its hidden supervisors. Do not redispatch just to inspect an existing batch.
Local attempt paths and sessions are in ignored
`active-batch.local.json`. `Run-Worker.ps1` bounds each child to 20 minutes and
records PID/start, actual exit, timeout/error and a best-effort Windows notice.
Native startup was verified for all five jobs in batch `20260910T004529Z`.
That batch actually contains S14 inputs (verify each input manifest); the prompt
contains a stale S13 label. An earlier Python-detached launch left no native
records or surviving supervisors and was not counted as completed worker work.
At most two coordinator polls are allowed per batch; ordinary supervisor waiting
does not invoke a model. Finished reports still require source/hash/test review.

## Git delivery

The feature branch starts from `origin/main` (`b4195ca`), outside the rejected
local `main` commit `54d0c7c`. Local main is preserved for recovery. Tool archives,
extracted binaries and caches under `studio/.local/` are ignored. Commit only
reviewed source/evidence; verify every outgoing blob before pushing the feature
branch. No force-push or published-history rewrite is needed.
