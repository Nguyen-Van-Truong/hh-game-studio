# Current-source functional queue after S132

AUTHORITY=0. Preparation only; no acceptance, engine dispatch or new gate.
The live host-memory workload must finish and retained exits/cleanup be checked
before any test or engine lane below is started. Use one writer/file and fresh IDs.

1. Read the host terminal result, seven or partial command/metric rows, first
   failure, HTTP phase window, actual target/helper/launcher exits and cleanup.
   Compare start, report-live, write-held and released working set/private commit.
   Preserve all raw bytes and known gaps before selecting a repair or next probe.
2. Run `validate_default_callers.py` once from this directory. It requires the
   completed host measurement and closed owner Job/handles. Six fixed modules
   cover default Job callers: Godot Linux CLI cleanup, validation binding, editor
   owner; Blender export cleanup and publication deadline; pipeline native Job.
   Capture actual unittest exits and skip counts, source map and raw stdout/stderr.
   These tests use mocks or bounded Python children, not Godot/Blender engines.
   S131's 22+170 tests need no repetition while their source dependencies match.
3. Compare accepted maps to current source once, caching each current file hash
   across maps. Anchors: S55 effective-execution-manifest; S60 execution-closure
   and coverage; S64 GT05 manifest; S79 backend174/UI5; S69 managed159/verifier4.
   List exact changed paths and unresolved dynamic dependencies. Old signatures
   remain historical. Do not label a broad map current from a narrow unit pass.
4. Implement and verify only the binding boundary described in
   `dependency-binding-design.md` once its complete dependency inventory exists.
   Keep immutable GT05 input hashes and strict current execution hash checks.
   No allow-stale switch, source guard catch/skip, altered GT05 publication
   verifier or caller-supplied source map may authorize execution.
5. Freeze a current candidate and refresh affected GT06 functional lanes:
   fixed asset import/input/pause/stop/observe/capture, authenticated service
   completion/Stop/saturation/revocation/stale capture, managed fault→repair→replay.
   Preserve the original S65/S69 causal evidence rather than relabeling it.
   Reuse UI-only or other narrow evidence only after exact dependency comparison.
6. Return to the prescribed full benchmark when the observed failure is addressed
   or a justified admission decision exists. No host-only result supplies F13/F14.
   Final freeze, requirement mapping, all attempts and two new independent critic
   PASS/TICK=yes on the same hash remain required before GT07.

GT03/S55 and GT04/S60 original native controllers execute their historical frozen
copies. Re-running them unchanged would not prove current shared Job/transport.
Their unchanged semantic/art evidence can remain historical; actual default-owner
integration must be covered in the new functional runtime closure where used.
Any claim of a new current GT05 publication still requires affected stage remints
through the existing strict pipeline verifier; replay does not authorize publication.

Known wrappers available for further targeted integration, once needed by the
dependency table: `studio/tests/godot/run_editor_owner_probe.py`,
`run_publication_probe.py`, `run_script_publication_probe.py`,
`run_publication_stop_probe.py`; `studio/tests/blender/run_export_cleanup_probe.py`
and `run_writer_client_probe.py`. Inspect their exact pinned source and supported
arguments at dispatch rather than copying historical IDs or changing old evidence.
