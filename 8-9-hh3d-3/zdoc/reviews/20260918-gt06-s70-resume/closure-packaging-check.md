# S70 campaign closure and packaging check

Read-only preparatory review, 2026-09-18 Asia/Saigon; not an acceptance verdict. Initial HEAD: `a007b21f5b7f630644d45b95be7a4e83cb63e678`. This review read the 47-file campaign map, its source copies, directly referenced dependencies and small supervisor records. No tests, engines, benchmark-module imports or raw-tree scans were performed. Only this report was written.

**Two concrete source issues were reported to the coordinator before the next freeze:** entrypoint-dependent Python closure caused campaign01 to stop before project creation, and the imported performance schema was absent. The coordinator is repairing the campaign-specific source union; the findings below bind the original S70 campaign01 package, not the changing live runner.

## Findings and existing coverage

1. **Parent/child closure mismatch is demonstrated.** `source_files()` in frozen `run_native_benchmark.py:97–113` enumerates imported local `.py` modules. The scheduler calls `run_campaign_task.py:82` in-process, so parent `__main__` adds that launcher to its 47-file map. The fresh host child executes `run_benchmark_campaign.py` directly and does not import the launcher. Its exact-map check at frozen runner lines 178/542 rejects with `CAMPAIGN_SOURCE_CHANGED`. Host stderr records that traceback, actual PID 51172 exited 1, and cleanup records closed/zero Job plus closed wrapper handle. Supervisor return reports code 1 after 1.422 seconds. No project/editor/sample was produced. Keep equality strict; use a campaign-specific stable union containing its fixed runner/launcher dependencies in both entrypoints.

2. **One imported data file is omitted.** `benchmark_profile.py:39` imports `host/replay/perf.py`, which reads/parses `contracts/perf-collector.schema.json` at lines 40–43 and checks it at lines 72–74. The schema is absent from the original map and both source copies. Include it in the campaign-specific source union and frozen copies. Its inspected SHA256 is `394d1df03bdfd736ea3fe9f260e9d718d35f3820cde35d1c45ccab65fbc24834`. This is an actual import dependency, not an extra test requirement.

3. **Launcher source and scheduler evidence have different bindings.** `run_campaign_task.py` is present in campaign01's map, request and supervisor start record. `campaign_task.ps1` is absent from that campaign map; the request's exact schema binds the Python launcher, pythonw and python, not the PS1 itself (`campaign_task.ps1:155–175,210–217`). The PS1 is nevertheless retained at the reviewed Git checkpoint and bound by `git-index-proof.json`; its bytes match `git show a007b21`. Include it in the next campaign source union and preserve its checkpoint copy. The registered XML/request readbacks already bind the effective scheduler action, arguments, principal and settings; they must remain in the final evidence package.

No additional actual file dependency omission was found in the bounded pass. Every resolvable static `studio.*` import was represented. Dynamic `cli_job.py`, `ipc_client.py`, `bundle_v2.py`, the fixture factory and fixture source-pins are represented. `BenchmarkProcess` uses the `HELPER` string embedded in bound `host/blender/ui_host.py:64–72`; `pipeline/native_job.py` uses the same bound helper. GDScript preload paths are covered. Fixture scene/script/project/input bytes are generated by bound source and captured through initial/project/native manifests. Blender adapter/recovery/export-probe files occur in dormant Blender branches; this campaign does not execute those branches. Added tests/diagnostics do not, merely by existing, invalidate previously accepted scoped lanes.

## Required final packaging supplement

The campaign capture chain does not itself include the sibling supervisor directory. Even after the source union is repaired, create a final review manifest with path, byte length and SHA256 references joining these existing evidence groups:

- Exact campaign ID, completed campaign/capture/dataset hashes, source closure, profile hash, frozen source copies, toolchain lock, fixture source-pins and per-run source/project manifests. Preserve unsuccessful prefixes separately.
- Both launcher source files and their reviewed commit/byte hashes; unchanged-source verification after execution. Keep diagnostic/test source and results in their own scoped evidence lists rather than rewriting historical benchmark provenance.
- For each scheduler launch: request, expected/registered task XML, XML hash sidecar, registration receipt, dispatch/instance receipt, single-use claim, supervisor start/return/failure, stdout/stderr, terminal scheduler status and deletion receipt if deleted. Bind the supervisor's campaign/launch number to the campaign manifest. `return.json` expressly precedes actual supervisor exit; it cannot replace terminal scheduler evidence.
- Pinned Python console and pythonw executable paths/hashes, Godot GUI executable/source commit/hash and lock; retain the effective XML action and interpreter selection. These external toolchain identities supplement the repository source closure.
- Checked host/editor process starts/exits, owner captures, natural-exit/zero/closed-handle evidence and native raw/readback links already required by the campaign. Scheduler success alone supplies none of those run-level proofs.

This is a packaging requirement for a reproducible final review, not a recommendation to change acceptance counts/profile or to import every unrelated test file into the runtime closure. The original campaign01 produced zero native samples, so a repaired freeze needs fresh evidence rather than relabeling its failed prefix.

## Reviewed byte anchors

Original campaign closure (47 files): `55d7a85f2fb43d1ba8b4aa719f89c96c571b5e278c0da4c76df6e5045a1aae1d`. Both campaign-level and attempt-level stored copies match all 47 hashes. The live runner changed during the coordinator's repair; it is not substituted for the frozen source reviewed here.

| Item | SHA256 |
|---|---|
| Original `campaign.json` | `cdba8d953ef4b69fbc26c0e0270ad4ddf1111a309501869e21bff4da7e057584` |
| Frozen `run_benchmark_campaign.py` | `643767a9366e9737fb228ca758137fe8f6d7a09228d1ea30ef8ecad62e160809` |
| `run_campaign_task.py` | `f5cb71fa96b8fd2f935ac933dcd5190cef2b3b6821f031bfe72becb2d360a6fd` |
| `campaign_task.ps1` at a007b21 | `405e2abfa39332b1313871763184fdda0755ff998ec83310e67e10cebfe2ebb9` |
| Supervisor `request.json` | `2d76e916598ac300969bd5937b4983d1e94815f975a96ad4a928171c3a9528fc` |
| Registered `task-definition.xml` | `16f44f9348cc07e576c4707f3f719091bcc1044ad0507e42f1bcb3b069946f93` |
| Host stderr / exact closure failure | `260de291bbaf4cba12d169a80c4d153e07c0d7d2d558e9a66224bea3eb612670` |
| Host actual exit 1 | `ff696704a0543f4007e1fdcdb365129c3ce9b2632aab406730049167e5a7c1c3` |
| Host `cleanup-001.json` | `31f58e29075f8f16bb2df0bb72471bfcd0df794b729d6497022554d84959cfc8` |
| Python console executable, recorded request pin | `5f7b89a612c9b8af1d6456cdfcd1dbe5ca630849e79aebced9bee9a6694952ec` |
| pythonw executable, recorded request pin | `d5355e1de2a5195dccb1ba524b146aa7705be71af18d876819756838251b37b3` |
| Godot GUI executable, toolchain lock pin | `ab1824f85bfd8e0e4128182c000c4003a3e042245b2967848d089b2a04b22424` |

Raw roots are `studio/.local/reviews/gt06-s70-campaign-01` and `studio/.local/reviews/gt06-s70-campaign-01-supervisor`. Existing S70 `evidence-inventory.json` covers six earlier diagnostic roots; it does not yet package this campaign or its supervisor. Preserve those earlier exact hashes and add a separate final campaign manifest after completion.
