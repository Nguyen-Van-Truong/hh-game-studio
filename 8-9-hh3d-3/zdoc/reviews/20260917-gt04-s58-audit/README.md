# S58 fixed publication profiles

All three profiles use source closure
`0785f748ee93882e38f4b261a6db3764271c7fbdb84508e6df00231df4e19efc` (138 files).
Scene save:571/571 all tests. Checkpoint/export:305/305 focused tests each.
Each real HTTP lane:47 client checks,34 calls,10 native checks,9 common command
pairs (8 committed and one known pre-effect capacity rejection). Actual GUI,
background validator, CLI, host and wrapper exits0; owned Jobs empty/closed.

Run `python -B verify_evidence.py scene`, then `checkpoint` and `export`.
These audits open no native handles. They bind common requests to typed private
commands, immutable publication profiles, protected .blend/GLB/manifest/selector,
exact ledger receipts and actual child exits. Public ACK and writable GUI
restart remain false. Legacy configuration without a profile still means export;
new configuration pins one closed profile. No arbitrary path or overwrite.

GT04 is not accepted. Remaining work: final shared-closure regression matrix,
file-operation preview/preflight review and two independent critics. Latest owner
steering is solo; self-review does not count as two independent signatures.
Scene01 is an earlier passing component closure before the known-capacity response
fix and catalog clarification. It is retained separately and never relabeled02.
