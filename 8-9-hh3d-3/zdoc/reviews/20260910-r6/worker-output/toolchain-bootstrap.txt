```json
{"files":[{"path":"build/bootstrap/acquire_toolchain.py","content":"import argparse\nimport json\nimport hashlib\nimport urllib.request\nimport zipfile\nimport shutil\nfrom pathlib import Path\ndef download_with_verify(url: str, dest: Path, expected_sha256=None, expected_size=None, timeout=30) -> None:\n    \"\"\"Download to .part then rename after verify. Reuse if already verified.\"\"\"\n    part = dest.with_suffix('.part')\n    if dest.exists():\n        if expected_sha256:\n            if hashlib.sha256(dest.read_bytes()).hexdigest() == expected_sha256:\n                print(f\"Reusing verified {dest.name}\")\n                return\n            else:\n                dest.unlink()\n    try:\n        req = urllib.request.Request(url)\n        with urllib.request.urlopen(req, timeout=timeout) as resp:\n            h = hashlib.sha256()\n            total = 0\n            with open(part, 'wb') as f:\n                while True:\n                    chunk = resp.read(8192)\n                    if not chunk:\n                        break\n                    f.write(chunk)\n                    h.update(chunk)\n                    total += len(chunk)\n                    if expected_size is not None and total > expected_size:\n                        raise ValueError(f'Excess bytes downloaded: {total}')\n        if expected_sha256 and h.hexdigest() != expected_sha256:\n            raise ValueError(f'SHA256 mismatch for {dest.name}')\n        shutil.move(part, dest)\n        print(f'Downloaded {dest.name}')\n    except Exception:\n        if part.exists():\n            part.unlink()\n        raise\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('--studio-root', required=True)\n    args = parser.parse_args()\n    studio_root = Path(args.studio_root)\n    lock_path = Path('toolchain.lock.json')\n    if not lock_path.exists():\n        raise FileNotFoundError('toolchain.lock.json not found')\n    with open(lock_path) as f:\n        lock = json.load(f)\n    godot = lock['godot']\n    export_t = lock['export_templates']\n    install_root = studio_root / godot['install_root']\n    install_root.mkdir(parents=True, exist_ok=True)\n    zip_url = godot['archive']['url']\n    zip_dest = install_root / godot['archive']['name']\n    zip_sha256 = godot['archive']['sha256']\n    download_with_verify(zip_url, zip_dest, zip_sha256, 86013866, 30)\n    sums_url = godot['sha512_sums']['url']\n    sums_dest = install_root / 'SHA512-SUMS.txt'\n    download_with_verify(sums_url, sums_dest, godot['sha512_sums']['sha256'], 5682, 30)\n    sha512_map = {}\n    with open(sums_dest) as f:\n        for line in f:\n            line = line.strip()\n            if line.startswith('#') or not line:\n                continue\n            if ' ' in line:\n                h, fn = line.split(maxsplit=1)\n                sha512_map[fn] = h\n    editor_fn = godot['archive']['name']\n    tpz_fn = export_t['name']\n    if editor_fn not in sha512_map:\n        raise ValueError(f'Editor {editor_fn} not in SUMS')\n    zip_sha512 = hashlib.sha512(zip_dest.read_bytes()).hexdigest()\n    if sha512_map[editor_fn] != zip_sha512:\n        raise ValueError('Editor SHA512 mismatch with SUMS')\n    tpz_url = export_t['url']\n    tpz_dest = install_root / tpz_fn\n    tpz_sha256 = export_t['sha256']\n    tpz_size = export_t.get('size_bytes', 1281349702)\n    download_with_verify(tpz_url, tpz_dest, tpz_sha256, tpz_size, 30)\n    tpz_sha512 = hashlib.sha512(tpz_dest.read_bytes()).hexdigest()\n    if sha512_map.get(tpz_fn) != tpz_sha512:\n        raise ValueError('Templates SHA512 mismatch with SUMS')\n    staging = Path('staging/godot-4.7.2-stable')\n    gui_exe = godot['gui_executable']\n    console_exe = godot['console_executable']\n    gui_path = install_root / gui_exe\n    console_path = install_root / console_exe\n    def verify_hash(p: Path, expected: str):\n        h = hashlib.sha256(p.read_bytes()).hexdigest()\n        if h != expected:\n            raise ValueError(f'{p.name} hash mismatch')\n    if gui_path.exists():\n        verify_hash(gui_path, godot['gui_sha256'])\n        verify_hash(console_path, godot['console_sha256'])\n        print('Already installed, hashes verified')\n    else:\n        if staging.exists():\n            shutil.rmtree(staging)\n        staging.mkdir()\n        with zipfile.ZipFile(zip_dest) as z:\n            for m in [gui_exe, console_exe]:\n                if '..' in m or '/' in m:\n                    raise ValueError('Invalid archive entry')\n                z.extract(m, staging)\n        shutil.move(staging / gui_exe, gui_path)\n        shutil.move(staging / console_exe, console_path)\n        for p, e in [(gui_path, godot['gui_sha256']), (console_path, godot['console_sha256'])]:\n            verify_hash(p, e)\n    receipt = {\n        \"schema\": \"HH-STUDIO-TOOLCHAIN-LOCK-2\",\n        \"revision\": lock.get(\"revision\", \"GT01-20260910-01\"),\n        \"godot\": {\n            \"version\": godot[\"version\"],\n            \"source_tag\": godot[\"source_tag\"],\n            \"install_root\": str(install_root.relative_to(studio_root)),\n            \"console_executable\": godot[\"console_executable\"],\n            \"gui_executable\": godot[\"gui_executable\"],\n            \"console_sha256\": godot[\"console_sha256\"],\n            \"gui_sha256\": godot[\"gui_sha256\"],\n            \"archive_url\": godot[\"archive\"][\"url\"],\n            \"sums_url\": godot[\"sha512_sums\"][\"url\"],\n            \"observed_sha256\": zip_sha256,\n            \"observed_sha512\": zip_sha512\n        },\n        \"export_templates\": {\n            \"version\": export_t[\"version\"],\n            \"name\": tpz_fn,\n            \"install_root\": str(install_root.relative_to(studio_root)),\n            \"url\": export_t[\"url\"],\n            \"observed_sha256\": tpz_sha256,\n            \"observed_sha512\": tpz_sha512\n        }\n    }\n    receipt_path = install_root / \"receipt.json\"\n    with open(receipt_path, 'w') as f:\n        json.dump(receipt, f, indent=2)\n    print(f\"Acquisition complete. Receipt: {receipt_path}\")\n    print(f\"Installed to: {install_root.relative_to(studio_root)}\")\nif __name__ == \"__main__\":\n    main()","notes":["unverified items"]}]}
```