"""Executable Grok workspaces, no canonical source write permission."""
from pathlib import Path
import json, shutil, tempfile, uuid, hashlib, datetime
HERE=Path(__file__).resolve().parent
STUDIO=HERE.parents[2]/"studio"
PREV=HERE.parent/"20260910-r5"
stamp=datetime.datetime.now(datetime.timezone.utc).strftime("%Y%m%dT%H%M%SZ")
batch=Path(tempfile.gettempdir())/("hh3d-executable-"+stamp)
tasks={
"runner":"""Fix ONLY studio/build/bootstrap/run_fixture.py and studio/tests/bootstrap/test_run_fixture.py in this workspace. Existing Windows job membership inheritance is CORRECT: assigned gated helper inherits to target/descendants. Do NOT move assignment after target spawn or query a closed job. Current Godot4.7.2 actually prints 4.7.2.stable.official.ed1daf0bf; DO NOT change to hyphen or fake supported format. Correct concrete defects: _observed_version ignores returncode/stderr and truncates a search regex; use exact full version from lock and retained bounded process job for --version. Full pin must match lock; caller hashes cannot override publisher pin. Validate untrusted paths BEFORE resolve: reject '..', links/reparse/ancestors/closure including dir junctions; output exclusively created and cannot overwrite. Manifest should include explicit runtime inputs (lock/bootstrap/fixtures/tests), ignore .godot/.local/pyc/evidence, snapshot copied files must match frozen fixture manifest before launch and source manifest rechecked after execution. Stop stage chain on log ERROR/WARNING as well as bad exit; parse GT01_TRACE with strict JSON exact required fields duplicate keys rejected. Test actual negative cases with otherwise VALID roots/files using mocks only for Godot executable, not the rejection implementation. Real process tests: nonzero exit preserves value, child outlives parent detected and killed (verify exact PID/handle independently), timeout tree zero, never target exit0 if missing report. Report missing target exit honestly on forced kill; preserve host metadata PID at launch before wait. Thread/process cleanup on helper creation/Job assignment exceptions. Runtime smoke is candidate only, full GT01 missing headed/Blender/rollback/two critics. stdlibPython3.11. Run python -m unittest discover -s studio/tests/bootstrap -p test_run_fixture.py -v, store stdout stderr actual exit. Do not call Godot or network for this task. End with REPORT.md listing changes/tests/limitations and evidence.json hashes/results, written on disk. Do not just describe patches.""",
"acquire":"""Write ONLY studio/build/bootstrap/acquire_toolchain.py and studio/tests/bootstrap/test_acquire_toolchain.py. Implement stdlibPython3.11 offline-verifiable pinned acquisition. CLI --studio-root. Parse lock from studio root (not cwd). Install root hardcoded .local/tooling under studio; reject traversal/root or ancestor symlink/reparse BEFORE resolve. Do not interpret lock install_root by joining studio/studio. Godot ZIP/SUMS/templates URLs only exact lock Godot4.7.2 github godotengine/godot-builds/download namespace; allow redirects only HTTPS release-assets.githubusercontent.com/objects.githubusercontent.com/github.com. Download exclusive .part, max exact size bounds, stream sha256 and sha512 (never read_bytes on1.28GB), verify exact length and lock sha256 before rename. Bad existing destination should raise, NOT delete. Parse SUMS with exact filenames + duplicate rejection; compare sha512 before extraction. Verify existing installed GUI and console hashes. For new installation verify archive exactly two expected exe files, no dir/symlink/traversal or oversized decompressed files; extract into unique same-parent staging, verify binaries then rename directory exclusively. No recursive deletes, no global cache/path edits, no trust by filename alone. Store portable local receipt hash/version/URL not username. Host will run real archives after review. Do NOT download anything in worker. Tests create tiny ZIP/fake download response, test checksum/size failure, traversal/symlink, existing target unchanged, stream happy path with source hash, wrong cwd. End REPORT.md and evidence.json with real unittest/py_compile output. Run tests yourself. File writes only permitted paths and reports/logs.""",
"blender":"""Write ONLY studio/fixtures/sample-blender/create_fixture.py and README.md. Trust only factory-startup background Blender5.2.1, no external inputs. argparse sys.argv after '--', --output <new .blend>. Reject existing output, output ancestors symlink/reparse BEFORE resolve; require existing parent and suffix.blend. Use tempfile directory under output parent for staging unique. No deletion user files or broad rmtree. bpy.ops.object.select_all+delete factory defaults first; create cube with size=1 centered origin, identity matrix, material simple diffuse, scene metric scale1. Blender is always Z up; system_rotation enum is DEGREES/RADIANS not Z_UP. Save staging with check_existing false only unique staging path, read back via open_mainfile; verify1mesh1object expected name unit bounds from vertices, identity world transform within epsilon,material/unit/version actual. Final publish to requested new path must not overwrite (os.link temp to output then unlink OWN temp on same volume works; Windows os.rename fails if target exists but portability explicit); do not second-save to output. print GT01_BLENDER_TRACE JSON after verified saved artifact, resultPASS, actual bpy.app.version_string, coordinates/mesh counts. No sys.exit(0) before normal Blender lifecycle; exception propagates --python-exit-code2. Candidate only not bridge. Validate with py_compile (Blender not available in worker; don't claim executed). Tiny pure path helper selftests using ast? Don't mirror implementation. Ensure no indentation error or nonexistent bpy enums. README exact command with --background --factory-startup --disable-autoexec --python-exit-code 2 --python script -- --output path. Write REPORT.md and evidence.json actual file hashes+compile exit, limitations. Do not report claimed bpy run."""
}
rows=[]
for name,task in tasks.items():
 attempt=batch/name; work=attempt/"workspace";work.mkdir(parents=True)
 for folder in ("build","fixtures","tests"):
  shutil.copytree(STUDIO/folder,work/"studio"/folder,ignore=shutil.ignore_patterns(".godot","__pycache__","*.pyc"))
 shutil.copy2(STUDIO/"toolchain.lock.json",work/"studio/toolchain.lock.json")
 full="You are a Grok implementation worker. ACTUALLY edit files and execute validation with your tools in this isolated workspace. No model/subagent delegation. Never write canonical repository, no Git commit/push or external projects. No permissions question. Work within7minutes. "+task
 (attempt/"TASK.txt").write_text(full,encoding="utf-8")
 cfg={"id":"hh3d-repair-"+name,"attempt":stamp,"session":str(uuid.uuid4()),"workspace":str(work),
 "notification_repo":str(STUDIO.parents[2]/"hoan-hao"),"turn_limit":20,"max_seconds":480,"notify":True,"web_search":False,"delivery":"FILES"}
 (attempt/"config.json").write_text(json.dumps(cfg,indent=2),encoding="utf-8")
 shutil.copy2(PREV/"Run-Worker.ps1",attempt/"Run-Worker.ps1")
 rows.append({"role":name,"attempt_dir":str(attempt),"workspace":str(work),"session":cfg["session"],"supervisor_pid":None,"task_sha256":hashlib.sha256(full.encode()).hexdigest()})
 record_source={p.relative_to(work).as_posix():hashlib.sha256(p.read_bytes()).hexdigest() for p in work.rglob("*") if p.is_file()}
 (attempt/"input-manifest.json").write_text(json.dumps(record_source,indent=2),encoding="utf-8")
record={"schema":"HH3D-WORKER-BATCH-1","created_utc":stamp,"batch_root":str(batch),"model":"grok-4.6","reasoning_effort":"xhigh","poll_budget":2,"polls_used":0,"jobs":rows}
(HERE/"active-repair.local.json").write_text(json.dumps(record,indent=2)+"\n",encoding="utf-8")
(batch/"batch.json").write_text(json.dumps(record,indent=2)+"\n",encoding="utf-8")
print(batch)
