# GT05 S62 foundation checkpoint

Candidate component work only; GT05 remains IN_PROGRESS and has no final critics.

The original producer creates a tiny 1 m/axis/PBR/avatar/outfit/LOD/idle/walk
fixture under a private disposable root. Bounded GLB, PNG, accessor and semantic
checks run in an owned Python Job before the pinned Khronos validator. Native
Godot diagnostics have passed 12 meshes, 12 bones, 792 sampled poses, 2,160 skin
vertices and same-project reimport preserving authored script/override/socket.
Those consumer files remain active work and are not included in this foundation
checkpoint's source map.

The captured foundation suite ran 134 tests: 133 passed, one skipped because this
Windows session cannot create symlinks. Target and wrapper exited 0; the owned
process tree was verified. The exact tested source and two later changes are
listed separately in checkpoint.json. The later validation driver change was
executed by validation-s62-01 with real bounded admission and Khronos processes;
all input, output, dependency and source hashes are retained.

Producer native05 and repeat01 GLBs have identical bytes. The edited crate is
wider and has a different GLB and semantic fingerprint. Five exact pinned Blender
exporter warnings are explained in pipeline/producer/CONTRACT.md and checked by
count/body; stderr is empty. Khronos has zero errors and zero warnings.

Keep every .local raw run listed in checkpoint.json. This portable hash index
contains no copied local paths or invented exits. Earlier failed attempts are
preserved. Validation-native04/repeat01/edited01 source snapshots were recovered
from exact hash-equal bytes after execution and are labelled as such; the new
validation-s62-01 makes its snapshots before launching either child.

Still required: finish original-PBR visual and draw-call proof, final evidence
verifier, protected staged publication with Stop/crash/reopen matrix, a single
frozen source/evidence package, Git byte proof and two independent final critics.
No active editor or game activation is part of GT05; that remains GT07.
