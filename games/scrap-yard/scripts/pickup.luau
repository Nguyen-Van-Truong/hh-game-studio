--!strict
-- Pickups are sensors; the fighter script applies the take. This file is a
-- no-op so a scene that still binds it stays inside the 4ms update budget.
local M = {}

function M.on_init(_self)
end

function M.on_update(_self)
end

return M
