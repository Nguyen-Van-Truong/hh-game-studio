# Gauge Deck — VF5-WP6

Display title: **Vault Fighters**. This document does **not** claim Y8 parity.

Internal id stays `gauge`. Display name is **Gauge Deck**.
Topology is `ledger:RL-MAP-GAUGE` (`assumption`), not observed Y8.

Training/test floor: wide low hall, marked loft rails, mid crate lane,
west cage door, and an observation lift. Wider/shorter than the four
Stage arenas (72×14). Unique live beat is boarding `gauge_lift` with
`apply_frames` (`auto_call_on_board`).

No water / toxic / rotor on this map. Clock is
`ledger:RL-SIM-FIXED-60` (`assumption`). Jump envelope is product tuning.
Art still VF7.
